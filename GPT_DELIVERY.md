# 给 GPT 的统一交接报告：系统现状、Round9 验证与历史报告交付

更新时间：2026-09-14。本文件汇总后续规划所需的主要事实，只需把这一份交给 GPT。原报告和 JSON 是需要逐条追溯时使用的底稿，不是理解本报告必须同时上传的附件。

## 1. 当前完成到哪里

系统已有服务器端数据下载与处理、历史批次自动回放、真实 Qwen 调用、工具查询及地图展示。两个案例的原业务目标仍未完成：哈尔科夫没有得到获支持的事前红色结果，霍尔木兹航运红色使用的是事件后的数据。

随后几轮做了不同工作，不能把它们合并理解成系统能力已经修好：

| 阶段 | 实际完成 | 原系统是否因此改变 |
|---|---|---|
| Round6 | 双案例自动回放、真实处理与模型工具分析，保存失败和修订记录 | 有代码和运行变化，但两例原目标仍未通过 |
| Round7 | 读取保存的请求、工具、材料和版本，核查历史结论 | 只读复核；新模型调用 0 |
| Round8 | 按复核结果更正交付文档中的解释和分类 | 文档更正；新模型调用 0 |
| Round9 数值验证 | 八题、每题 A/B 两条件，收到 Qwen3-4B 共 16 份真实回答 | 独立验证，未接入原应用 |
| 最新历史报告交付 | 新增独立生成器、两份静态事实报告及统一入口 | 独立离线产物；新模型调用 0，8080/8081 未修改 |

最近实现提交为 `5f6b2b9`，内容是最后一行的离线报告包。本交接整理本身没有再次运行模型、访问服务器或更新在线页面。下述服务器和业务状态来自最近保存记录。

## 2. 系统自动化和 LLM 的实际作用

历史案例会按已配置的来源、日期、站点和批次推进，复用服务器已有文件与特征，也有实际下载记录。它不是纯粹把一张手写结果表展示在前端。

Qwen 也确实用过：原请求保存了模型选择工具、信号、时间窗口和事实引用的过程；同站历史比较、多站同窗比较都有真实返回。有些回答在读到工具结果后改正了原句，有些仍未解释完整。哈尔科夫首黄的同站结果存在重用，多站工具则实际执行；不能把每次引用都算成新计算。

但已保存案例没有证明完成了“模型发现证据缺口 → 自主确定新来源或扩大下载范围 → 下载新增资料 → 重新分析并持续追踪”的完整闭环。现有自动下载和对已有材料的工具查询，需要与这个能力分开评价。也不能用历史批次跑完来证明长期在线订阅已经可靠运行。

数值计算与业务状态也有明确分工：窗口统计和航运指标由程序计算；霍尔木兹航运红色来自既有连续低通行量规则。模型参与事实选择和解释，不能把程序标红全部记为模型发现。当前没有证据证明 Qwen 让两例结果提前。

## 3. 两例的真实结果与关键问题

事件日期沿用原历史注释，没有在本次重新核实；精确时刻和时区仍未确定。观测日期、模拟回放截止、现实中的分析时间是不同字段。

| 案例 | 已保存结果 | 原目标为什么仍未通过 |
|---|---|---|
| 哈尔科夫 | 首黄的回放截止为 2024-05-10 00:00 UTC；当前保存结果仍为黄色 | 事件注释为 2024-05-10，只有日期，无法确定同日先后；没有获支持的事前红色结果 |
| 霍尔木兹 | 2026-03-01 船次 18，在 03-02 截止显示黄色；03-02 船次 4 连同前日数据，在 03-03 截止显示红色。最后观测为 03-08 船次 2，截止为 03-09 | 原事件注释为 2026-02-28。上述红色使用的观测发生在事件后，不能证明事件前预警 |

两例均保留 `original_goal_status=not_demonstrated`、`strict_lead_hours=null`。后来把报告写清楚，没有改变这两个字段。

### 哈尔科夫：已有数值、参考和解释缺项

- 实际首个引用窗口为 **2024-05-08 23:59:42 至 05-09 00:00 UTC**。BUCU00ROU、C:S1P 的载噪比 P10 为 **43.78 dB-Hz**，采样间隔 30 秒；后一天约 42.6 的值属于另一窗口。P10 是窗口内较低一端的信号质量统计值。
- 两个参考组分列：2023-05-10 一日参考 **46.78**；2024-04-10、11、12 三日参考 **45.99～47.05**，中位数 **46.84**。首窗分别低于单日参考 3.00 dB、三日参考下界 2.21 dB。
- 两组各有 289 个配对窗口。2023 年组为 **136 低、153 高**，逐窗差值中位数 **+0.150 dB**；4 月组为 **104 低、89 高**，中位数 **−0.565 dB**。这是已保存汇总。先逐窗求差再取中位数，与两个整体中位数相减是不同计算。
- 首窗只有 **18 秒、1 个历元**，有效比例为 **8 / (1 × 29) = 27.59%**。29 来自整份文件中该信号出现过的卫星集合；历元覆盖比例 1.0 对应这个已定义窗口。
- 同窗 JOZE00POL 为 **42.4368**，高于本站 4 月参考 **25.0788～40.0922**。BUCU 同窗低、JOZE 同窗高；其他已保存例窗也出现过同向变化。模型调用并引用过多站材料，但没有完整回答站间关系。
- GFZ 5 月 8、9 日环境资料已出现在四次实际请求中，两日 Ap 都为 5。最终没有形成对应环境解释，缺的是解释而不是这两日输入。
- K-C3 中用小的中位差推出“没有窗口超出参考”的初答，已被同一次运行的末答修正。这个中间错误不能继续算成当前末答错误。

### 霍尔木兹：三条末答矛盾与航运问题

以下时间是原回放截止，不自动代替具体观测子窗时间。各站使用自己的参考，GNSS 与航运字段分别解释。

| 已确认问题 | 已有事实 | 仍留在历史末答中的问题 |
|---|---|---|
| 03-03 10:30，H-C5 第一条 | BSHM 首窗 **22.719 < 29.4848**，末窗 **22.219 < 33.469**；KITG 首窗 **38.25 > 33.4284**。右侧为所给单侧参考边界 | 第 5 次回答的缺口仍写“未提供足够的参考样本以明确该信号的异常是否超出有限样本范围” |
| 03-05 10:15，H-C5 第二条 | BSHM 首窗 **32.4875 < 33.9063**、末窗 **24.438 < 33.125**；KITG 首窗 **31.6875 < 34.1533**。汇总为 BSHM 四窗低、KITG 前两窗低后两窗高 | 第 5 次以没有显著性检验为由，说无法确认是否超出给定范围，并把问题归为缺观测；这些大小关系已有数值可算 |
| 03-05 10:30，H-C5 第三条 | 同一首窗 **10:14:42～10:15:00 UTC**，BSHM **19.9935** 低于 **29.4848～33.3967**，KITG **37.5217** 高于 **33～33.4284**；两站同 C:S1P、dB-Hz、1 秒采样。汇总为 BSHM 四窗低、KITG 四窗高 | 第 5 次仍写“现有观测数据在有限样本范围内，未超出参考范围”，并称单位和采样一致性未完全验证 |

另有以下已核事实：

- NICO 存有 **900 个历元、17 个信号**，原强度 **40 receiver_units**；`cnr_p10=null` 是未确认到 dB-Hz 的换算依据。它不表示原值为零或没有观测，也不影响另外两站已确认的单位。
- 3 月 2 日两批实际工具选择 **C:S5P**，其余已核 12 批选择 **C:S1P**。存在信号切换，但已审查回答中未找到明确把二者拼成连续趋势的原句。
- 3 月 6→7 日名义运力为 **143→102,414**，增加 **102,271**，增幅 **71,518.18%**；最终值仍低于两个历史日值 **3,044,613、1,314,529**。旧答写“高于历史平均”确实错误，后来修订改谈船次 **2→3**、历史 **69→67**，仍未答完原运力问题。
- 3 月 6、7、8 日船次分别为 **2、3、2**，因此前一段 2→3、后一段 3→2 可以同时成立。共享的 3 月 7 日原材料及版本一致，值均为 3。
- v6 的解释修订没有覆盖后来的 v7，也没有登记为物理恢复。这个保存关系本来就是正确的。

## 4. Round9：16 份真实模型回答的结果

执行于 **2026-09-14 12:38:58～12:39:18，北京时间**，约 20.29 秒。返回模型为 **qwen3-4b**；固定 temperature=0、max_tokens=1800、enable_thinking=false、JSON 输出。八题各调用两次：A 只给表和问题；B 给相同内容并附程序计算事实。没有在这之后追加请求争取全通过。

| 八题结果 | A：直接读表 | B：附带程序事实 |
|---|---:|---:|
| 完整通过 | 0 | 2 |
| 明确事实或逻辑错误 | 4 | 1 |
| 未答完整 | 4 | 4 |
| 待复核 | 0 | 1 |
| JSON 符合约定 | 8 | 8 |
| 引用行编号存在 | 8 | 8 |

| 题目 | A 的具体结果 | B 的具体结果 |
|---|---|---|
| N1 单侧边界 | 三处大小方向全反 | 完整通过；三处方向正确，未补造另一侧边界 |
| N2 例窗与汇总 | 有数字却说大小关系未明确，未答清窗口方向 | 例窗方向正确；漏四窗分组，“观测的下界”用词含混，保留待复核 |
| N3 两站异向及元数据 | 一低一高正确；漏信号、单位、1 秒采样、同窗和原因缺项 | 同样未答完整，仍漏具体元数据 |
| N4 名义容量 | 增量正确，增幅错写 **714.43%**；未说明最终绝对水平更低 | 完整通过，增幅 **71,518.18%**，最终值低于两个历史值 |
| C1 参考组与中位差 | 两组中位差及核心解释正确；漏参考天数和上下偏计数 | 同样未答完整，不能把原历史已改的错误重新算成当前错误 |
| C2 有效比例 | 8/29、18 秒和窗口内无缺历元正确；漏分母 29 的来源 | 27.59% 正确，仍未解释 29 来自整份文件卫星集合 |
| C3 单位及空值 | 保留原值 40，未答清换算、零值与观测存在的区别 | 补了换算缺项，仍未交代 900 历元和 17 信号 |
| C4 相邻日期 | +1、−1 算对，却说存在数值冲突 | 仍把 2→3、3→2 的方向不同说成冲突 |

全部 16 份原答已在原验证中阅读并保存。检查内容为数值关系、是否答完问题、字段含义、是否添加无依据的推断、引用是否对应输入。接口报告用量为输入 7,459、输出 1,191，合计 8,650 tokens。之前两次直连未收到模型回答，合计 HTTP 尝试 18 次，真实答题仍为 16 次。

当时模型服务监听 `.16` 的 `127.0.0.1:8000`，通过临时 SSH 转发访问同一服务；结束后转发已关闭，未改服务器监听。

这些结果只说明这八个匿名简化问题上的表现：程序事实帮助了 N1 和 N4，日期关系误判和回答遗漏仍存在。没有据此验证原历史案例修复，也没有证明其一般准确率或根因。

## 5. 最新一轮实际交付的离线报告包

最新工单明确限定为已有资料的独立报告交付，因此实现没有进入原应用的采集、工具循环、规则或模型提示词。

新增生成器位于 `tools/archive_observation_report/`，包含 build.py、facts.json 和 README.md。产物位于 `outputs/archive_observation_report/`：

- index.html：统一入口；
- kharkiv.html、hormuz.html：两份历史事实报告；
- results.json：原值、计算关系、来源定位、原话和字段缺项；
- DELIVERY.md：该静态产物的构建与交付记录。

页面按“对象与时间 → 原值和参考 → 计算关系 → 展开的来源与原话”呈现。无模型或在线资源依赖，复制整个输出目录即可本地查阅。本次程序生成的事实说明有独立署名，历史 Qwen 回答原样保留。

本轮保存 16 个来源、35 行事实和 18 段历史原话。BUCU 首窗、双参考、覆盖定义、多站例子及日记录来自已存结构化摘录；霍尔木兹范围比较复用 Round9 数值输入及原映射。289 窗汇总和四窗方向计数沿用来源声明，没有重新读取完整原件或绘制完整连续曲线。

已实际构建并通过本组件的数值、字段来源和包内链接检查。应用内浏览器拒绝打开本地 HTML，**实际打开与点击折叠区尚未检查**；这个未完成项保留在交付记录里。

在仓库根目录重新构建的命令为：

```powershell
& 'D:/anaconda3/python.exe' -B tools/archive_observation_report/build.py --output outputs/archive_observation_report
```

它只生成独立产物。本交接文件位于仓库根目录，不会被上述构建覆盖。原系统、原模型回答、原判断及两例验收状态没有因这次报告生成而改变。

## 6. 后续规划需要保留的用户要求与未完成项

用户希望后续工作回到系统运行能力，而不只是继续整理离线报告。待规划的问题包括：已确认的模型数值解释错误与遗漏如何在应用中解决，采集如何与实际证据需求衔接，新增资料如何进入后续分析，以及怎样展示清楚可追溯的结果。上述能力不能由报告页面的正确文字代替。

两例历史结果应按第 3 节已有事实继续评价。用户明确不接受使用事件后的航运下降来交付“提前预警”；已知后来发生事件也不能直接用作此前判断的证据。精确事件时刻和相关资料版本在历史截止前是否可得，现有记录没有充分回答。

以下持续要求同时影响规划：

- **说人话。** 对用户先给明确事实和简短证据，例如站点、历史参考、本次载噪比、变化量；不把内部重试、完成计数和实现限制堆进主卡片。新工单中的文案要求与此冲突时，按这一表达要求处理，具体缺项仍需如实写明。
- **界面保持通用。** 原历史案例使用同一地图布局、案例切换和可点击结论卡；详细证据与报告按需展开。GNSS、航运等是资料来源，不应各自变成完全不同的主界面。此前授权先改案例回放，一般监测页面需按后续明确范围处理。
- **运行依靠 `.16`。** 笔记本用于访问和控制，不作为长期原始数据仓库。只操作本项目文件和服务，不改变其他用户的文件和环境。
- **服务器缓存要可解释。** 最近 Round6 记录沿用：成功计算的受管原件 72 小时，异常关联及失败原件 7 天，残片 24 小时，完整特征 90 天，普通日志 30 天；必要引用窗口、日表、参考摘要和报告长期保留。原件清理每小时，日维护北京时间 03:30；受管额度 50 GiB、高水位 45 GiB、回收目标 35 GiB。复用的 144 份 GNSS 原件未转入本轮自动清理对象。这是既有配置记录，不能说已观察到所有保留期自然到期。
- **操作聚焦。** 按用户已授权范围做必要修改和核对，不增加无关验证、smoke test、模型重复实验或新框架。普通实现问题自行修补；确实无法决定的事项再向用户提出具体问题。

仍缺的材料或解释，应具体区分：部分子窗精确起止和采样字段、JOZE 参考日期、NICO 单位换算依据、部分原字段绝对单位；多站和环境解释未完成；三条 H-C5 末答矛盾、名义运力原问题仍在。部分数值和元数据已经存在，不需要把解释失败一律变成重新下载原件。

## 7. 旧交接是否遗漏，以及本文件的依据

本次核对了现存各轮主报告。Round6～9 主报告已经包含其核心完成状态、问题和结果摘要；例如 Round9 的逐题 A/B 结果就在主报告中。未附全量 JSON 不等于遗漏这些结论。

也发现两处应明确的历史问题：

1. Round4 把完整服务器目录和缓存期限放在 SERVER_DATA_REQUIREMENTS.md。如果当时只传 ROUND4_DELIVERY.md 并要求规划存储，具体存储条款没有全部包含在那一份主报告里。数据未备齐和真实回放未完成的主要结论当时已经写明。上节现已带上较新的缓存配置摘要。
2. Round5 使用了“两例业务均已完成”等过宽措辞，而同一报告又列了专业工具调用为 0、哈尔科夫未发布异常。它混淆了回放执行完成与原目标通过；Round6～8 已明确两例原目标未通过。本文件沿用后者。

不能仅凭当前本地文件还原用户过去实际发送给 GPT 的所有内容，因此不宣称每次旧交接都完全没有遗漏。最近一次让我方建议用户同时上传两份报告，原因是静态包 DELIVERY 未整合上一阶段模型验证摘要；本文件已补上，用户不必自行拼接。

以下为仓库内的来源定位，供需要追溯时使用，本报告已包含规划所需摘要：

| 本报告内容 | 原材料位置 |
|---|---|
| 系统、下载缓存与 Round6 未解问题 | ROUND6_DELIVERY.md，“实际结果”“数据范围与解释”“尚未完成的具体项”“准确启动与续接” |
| 历史时间线、真实模型作用、三条矛盾和版本 | reports/round7/ROUND7_DELIVERY.md，第 2～6 节 |
| 当前错误、已改中间错误、未找到原句的区分 | reports/round8/ROUND8_DELIVERY.md，第 2～5 节 |
| 16 份 A/B 回答、计数和实际运行 | reports/round9/ROUND9_DELIVERY.md，第 1～5 节 |
| 逐题原始响应与检查意见 | reports/round9/ROUND9_NUMERIC_EXPLANATION_RESULTS.json |
| 独立静态报告的实际交付记录 | outputs/archive_observation_report/DELIVERY.md、results.json |
| 更早的存储条款分散和完成措辞 | ROUND4_DELIVERY.md 第 11、132 行附近；ROUND5_DELIVERY.md 第 119～125、217 行附近 |

本文件替代“需要同时给 GPT 两份主报告”的交接方式，不覆盖任何旧原答、旧结果或原应用状态。
