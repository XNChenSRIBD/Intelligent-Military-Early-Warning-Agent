# 第五轮交付：服务器数据获取、共享缓存与双案例续接

起始提交：`4639f86724e52120ed22152d1f10cc0857f28c91`。代码结束提交：`8c489c41310cebfde7db510d7ee47215cd917723`；本文作为随后独立的交付记录提交。

本轮接通服务器资源入口、原件与结果复用、来源获取、单文件计算、原回放实例续接、在线 GNSS 订阅和“数据获取与存储”页面。**代码交付与服务器只读盘点已完成；尚未部署或执行两例业务运行。** 本文的文件存在数来自服务器文件属性，不能替代解析、模型分析或业务验收。

## 1. 交付与执行状态

| 项目 | 本轮实际状态 |
|---|---|
| 代码、配置、启动脚本与前端 | 已实现，沿用现有 FastAPI、SQLite、GNSS 统计和 Qwen 工具循环 |
| `.16` 定向只读盘点 | 已完成：清单精确路径、原件大小、现有应用与进程、限定 PortWatch 归档范围 |
| 部署、独立依赖环境、应用重启 | 未执行，线上 8080 仍为原部署 |
| 来源下载、GNSS 解码与计算 | 未执行；服务器原件可定位不等于本轮已复用或处理 |
| Qwen 业务分析、专业工具与异常发布 | 未执行，没有本轮请求、响应、工作或异常记录 |
| 缓存回收 | 未执行，没有实际释放量或到期行为记录 |
| 浏览器截图、测试、smoke test | 未执行，没有本轮截图或通过记录 |

## 2. 已确认的服务器位置与现有启动方式

只读盘点时间为 **2026-09-12 12:26:52—12:32:39 UTC**。

| 用途 | 已确认事实 |
|---|---|
| 当前 Web 根 | `/home/xnchen/projects/Intelligent-Military-Early-Warning-Agent`；无 `.git`，现有 tar 包部署 |
| 现有在线数据 | 上述根目录的 `runtime/`；`demo.sqlite3` 为 139,264 字节，另有 `materials/`、`server.log` |
| 原件根 | `/home/xnchen/gnss_public_data/chasing_lightning` |
| 当前 Web Python | `/home/xnchen/miniconda3/envs/qwen3_4b/bin/python`，实际执行文件为该环境的 `python3.11` |
| 当前进程 | Web PID `3888267`，命令为上述 Python 加 `-m app`，工作目录为 Web 根 |
| 当前常驻方式 | 父进程 PID `3888264` 为 Bash，已确认启动命令含 `nohup`，输出至 `runtime/server.log`；进程处于会话 scope |
| 当前访问地址 | `http://10.20.14.16:8080/`；启动日志记录同一 PID 和监听地址 |
| 现有模型配置 | `http://127.0.0.1:8000/v1`，模型名 `qwen3-4b`；本轮未调用或操作模型服务 |
| 新运行根 | `/home/xnchen/gnss_public_data/early_warning_runtime`，盘点时尚不存在，其下新 catalog 亦未建立 |
| 独立工作台环境 | 目标为 Web 根下 `.venv-workbench/`，本轮未创建 |

现有 Web 共用模型环境，GNSS 依赖应安装到新的独立工作台环境。没有配置或确认开机自启，`nohup` 只对应脱离当前 SSH 会话运行。

## 3. 双案例输入：服务器可定位与实际处理分开计数

以下仅对 `cases/replay/kharkiv/manifest.json`、`cases/replay/hormuz/manifest.json` 中 `kind=gnss` 的精确相对路径逐项读取文件属性，没有解压、正文读取或 GNSS 解析。

| 案例与用途 | 需求数 | `.16` 普通文件存在数 | 精确路径缺失数 | 实际文件字节 |
|---|---:|---:|---:|---:|
| 哈尔科夫观测，2024-05-08～10 | 32 | 32 | 0 | 85,689,699 |
| 哈尔科夫参考，2024-04-10～12 及 2023-05-08～10 | 58 | 58 | 0 | 161,754,925 |
| **哈尔科夫合计** | **90** | **90** | **0** | **247,444,624** |
| 霍尔木兹观测，2026-02-27～03-05 | 42 | 42 | 0 | 39,568,908 |
| 霍尔木兹参考，2026-02-11、02-26 | 12 | 12 | 0 | 13,303,314 |
| **霍尔木兹合计** | **54** | **54** | **0** | **52,872,222** |
| **两例合计** | **144** | **144** | **0** | **300,316,846** |

哈尔科夫清单为 11 个实际站点的标称 30 秒日文件；霍尔木兹为清单三站每天 10:00、10:15 的标称 1 秒切片。实际采样、时制、有效历元与 CNR 单位仍须由本版处理读取 RINEX 确认，不把名义文件覆盖称为实测覆盖。

两例本轮通过资源入口发生的**原件复用、兼容结果复用、实际下载、成功解析均为 0**，因为尚未启动该业务链路；90/54 是可定位输入数，不是已处理数。没有本轮来源请求或解析失败记录，也不能据此将错误数解释为业务已通过。完整特征缓存的兼容复用数量待资源入口实际登记后确定。

仓库已有 45 份辅助 JSON 继续作为输入：20 份 PortWatch、8 条新闻标题、7 份 GPSJam、10 份 GFZ 日资料；标题未扩写为正文。

### PortWatch 前置 15 日

目标为 `chokepoint6` 的 **2026-01-30～02-13**。本次限定只读范围找到 **0/15 日**：

- 现有 `runtime/demo.sqlite3` 只有 `records`、`materials`、`run_materials`，尚无 `portwatch_daily`。其中 3 条 `source=portwatch` 材料所指日期为 2026-09-04、05、06。
- 线上 `cases/iran-hormuz-2026-02-28/results/freight_case_report.json` 仅列 02-23～02-27 五日日表，均不属于目标窗口。
- 已知资产根下 `external_intel/` 当前不存在；针对该位置、最多三层、文件名含 `portwatch` 的限定搜索没有候选。

此结论仅覆盖上述已知数据库和目录，不推断整个服务器不存在其他归档。仓库 02-14～03-05 的 20 日日表中有 13 日位于回放起点前；完整参考窗口还缺 15 日，距至少 21 个有效日的要求名义上还差 8 日。这两种口径不能混用。**本轮未补取或实际合并，最终有效参考日数与 `pw_lowflow_v1` 判定均待运行。** 来源首次发布时间未知的历史快照继续保持未知。

## 4. 实际代码通路

| 主要位置 | 本轮职责 |
|---|---|
| `app/resources.py` | 新增共享资源事实、消费者依赖、协调获取与计算、结果身份、重试和缓存策略；沿用现有 `app/store.py` 业务存储 |
| `app/acquisition_sources.py`、`app/sources.py` | 按登记 BKG/CDDIS 来源精确获取，复用现有 IGS 下载器能力；来源目录发现和绝对日期 PortWatch 获取 |
| `app/gnss.py` | `compute_raw()`、`load_raw_cache()` 将原始五分钟特征与上下文比较分开；原件不存在时按兼容身份读取缓存；`contextualize_result()` 按当前范围附加参考 |
| `app/replay.py` | 资源缺失转为持久需求，补到资源或参考后恢复原实例；批次输入版本、历史检查点、原截止时间重算与旧结果保留 |
| `app/online.py`、`app/pipeline.py`、`app/llm.py` | 已配置 GNSS 订阅结果进入现有工作队列与 `assess_update()`；同站历史、多站描述和实际材料工具继续受范围限制 |
| `app/locks.py`、`app/main.py` | 在线与回放共享 `catalog/model.lock`；应用生命周期、资源管理和维护接口 |
| `app/config.py`、`.env.example`、`scripts/start_server_data.sh` | 服务器目录、启用开关、同一应用的在线／回放启动入口 |
| `app/static/index.html`、`app.js`、`styles.css` | 数据获取与存储入口，订阅、两例需求、实际字节、存储分项、保留策略、清理与原件重取；保留原三栏看板 |

资源按已有原件、兼容本版结果、登记来源下载的顺序处理，共享协调入口限制下载 2 个、解析 1 个。原始特征按字节身份、处理版本和解释元数据复用；参考比较按消费范围重算。历史实例保留原实例、案例、批次及不可变 `as_of`，资源或参考变化生成新输入版本，从受影响检查点顺序恢复并保留旧结果；获取时间变化和缓存清理不触发业务新版本。

持续订阅只使用已保存的站点与范围，默认高频检查 15 分钟、日文件 1 小时。新特征入现有分析队列，无明确可比参考时保留参考不足。**本轮没有配置或运行持续订阅，也未观察资源迟到后原实例续接或基线更新事件。**

## 5. 准确启动方式：以下步骤尚待授权执行

保留线上 `.env` 与现有 `runtime/`，按既有部署方式更新工作台代码。先将 8080 在线应用升级为 R5，再启动回放，两者才会共同使用新增模型锁。旧在线进程没有采用新锁，不能承诺它与新回放天然串行。脚本不会停止旧 PID、迁移数据库或重启模型。

在服务器 Web 根目录准备**独立工作台环境**：

```bash
cd /home/xnchen/projects/Intelligent-Military-Early-Warning-Agent
python3 -m venv .venv-workbench
.venv-workbench/bin/python -m pip install -e . -r requirements-gnss.txt
```

授权替换旧在线进程后，在线前台启动命令为：

```bash
ACQUISITION_AUTOCLEAN=false bash scripts/start_server_data.sh online
```

在线默认继续使用当前 Web 根的 `runtime/`、端口 8080。为沿用已确认的 `nohup` 方式，可在旧进程已按部署授权处理后使用：

```bash
nohup env ACQUISITION_AUTOCLEAN=false bash scripts/start_server_data.sh online >> runtime/server.log 2>&1 < /dev/null &
```

双案例前台自动回放命令为：

```bash
ACQUISITION_AUTOCLEAN=false bash scripts/start_server_data.sh replay
```

该命令默认使用独立数据目录 `/home/xnchen/gnss_public_data/early_warning_runtime/replays/round4`，按批次调度哈尔科夫与霍尔木兹，复用两份仓库清单和既有模型接口；一个案例等待资源时，另一个仍可推进。默认回放端口 **8081 只是启动配置提案，本轮未确认其空闲或获得占用授权**；仅在该端口获准后使用，相应访问地址为 `http://10.20.14.16:8081/`。

需要后台常驻时，在相同授权条件下可执行：

```bash
nohup env ACQUISITION_AUTOCLEAN=false bash scripts/start_server_data.sh replay >> runtime/replay-server.log 2>&1 < /dev/null &
```

复用某个已存在的历史实例时，指定其**原目录**，不删除目录、不换实例来跳过未完成工作：

```bash
DATA_DIR=/实际已有回放实例目录 ACQUISITION_AUTOCLEAN=false bash scripts/start_server_data.sh replay
```

此处目录须替换为实际历史实例路径；本轮未在已查位置找到已运行的 R4 实例。新目录首次启动才建立实例，之后同目录重启续接。`SERVER_ASSET_ROOT`、`SERVER_RUNTIME_ROOT`、`WORKBENCH_PYTHON`、`APP_PORT` 可显式覆盖；旧 `.env` 不由示例覆盖。以上命令将自动清理明确关闭，缓存回收取得单独授权后再启用。

## 6. 存储、保留与页面

新受管原件与临时区共享额度 50 GiB，高水位 45 GiB，容量回收目标 35 GiB。正在使用或结果尚未保存的文件不参与回收；可回收量不足时等待缓存空间，其他可用来源与分析继续推进。

| 内容 | 默认保留与时点 |
|---|---|
| 停止任务残片 | 最后写入超过 24 小时，每小时清理；活跃任务保留 |
| 解压文件和中间数组 | 当前处理所需，成功保存或失败结束后释放 |
| 成功计算压缩原件 | 72 小时，每小时检查 |
| 异常关联原件 | 从计算完成／首次关联较晚时刻起 7 天 |
| 未处理或失败原件 | 最多 7 天；先保存缺口与重取依据，活动文件不删 |
| 完整五分钟特征 | 90 天，活动基线和未完成任务所需结果继续保留 |
| 普通日志及未引用冗长响应 | 30 天，与完整特征每日 Asia/Shanghai 03:30 处理 |
| 日汇总、基线摘要、日表、已引用窗口、异常、报告及关键工具依据 | 长期保留，不参加原件 TTL |

新下载原件可按台账纳入受管范围；本次找到的旧原件默认 `managed=false`，不会自动接管清理。受管原件、临时文件、完整特征、数据库、证据、报告及未接管原件分别显示，50 GiB 不是项目全部空间上限。当前新 catalog 尚未运行，**没有实际受管占用汇总或清理数量**；300,316,846 字节仅为上述 144 个旧文件总量。

“数据获取与存储”页面只在打开该入口时轮询摘要，资源详情和材料按需读取。未知下载总量显示未知；原件复用、结果复用和实际下载分列。订阅、重试、补缺、重取和清理按钮提交服务器任务，正常推进不依赖浏览器。清理后的资源保留原件身份、清理原因、指标和报告入口。

## 7. 两例业务结果与最小剩余条件

哈尔科夫、霍尔木兹均未启动本轮业务回放，因而没有运行起止时间、实例／批次／工作／异常 ID、真实模型请求响应、专业工具轨迹、结论或截图。模型配置名为 `qwen3-4b`，不能据配置称其已经完成分析。两例业务验收均尚未执行，霍尔木兹另需完成前置 PortWatch 获取与实际参考合并。

继续执行所缺的最小条件集中如下：

1. 授权建立独立工作台环境并安装应用／GNSS 依赖。
2. 授权更新现有 Web 代码、保留原配置与在线数据并进行必要应用重启；确认回放实例获准使用的端口。
3. 授权 PortWatch 前置 15 日的指定范围获取，以及需要时的登记来源正常补取。
4. 授权两例真实 GNSS 解析和 Qwen 业务回放，依据实际错误完成必要修复与局部续跑。
5. 受管缓存回收另行授权；未授权时保持 `ACQUISITION_AUTOCLEAN=false`。

上述事项不包含模型服务启动、停止、环境升级或 GPU 配置变动。GNSS 原件在已知服务器目录均已找到，当前不需要用户手工把本机原文件搬到服务器。
