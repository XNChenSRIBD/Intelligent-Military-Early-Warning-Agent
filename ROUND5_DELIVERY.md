# 第五轮交付：服务器数据获取、共享缓存与双案例续接

本轮已部署服务器获取、共享资源台账、GNSS 计算、原实例续接、在线订阅接线和数据管理前端。**两例均已完成真实自动回放：哈尔科夫 3/3 批、霍尔木兹 7/7 批，144 份 GNSS 文件解析成功、36 条工作完成。** 霍尔木兹发布并更新了一条 PortWatch 数值规则异常。两例与资源依据取自 2026-09-12 14:51:26 UTC 的最终记录，在线限制补至 14:53:05；实际专业工具调用为 0，页面截图未取得。

## 1. 提交、文件与完成范围

起始提交：`4639f86724e52120ed22152d1f10cc0857f28c91`。R5 主体提交：`8c489c41310cebfde7db510d7ee47215cd917723`。代码终点：`ea2915aceb556d4d5fb9e53a34d30a4128f38025`；部署后端为 `39992e7`，静态前端为 `ea2915a`。报告终点为包含本文的 R5 报告提交。

| 主要文件 | 已实现并部署的职责 |
|---|---|
| `app/resources.py` | 共享资源、获取与计算任务、消费者依赖、结果身份、重试、保留与清理 |
| `app/acquisition_sources.py`、`app/sources.py` | BKG/CDDIS 精确来源获取、订阅发现、PortWatch 绝对日期补取 |
| `app/gnss.py` | gzip/Hatanaka 解码、实际采样与单位记录、五分钟统计、兼容结果读取、按当前参考范围比较 |
| `app/replay.py` | 资源到达后续接原实例、输入版本与检查点、保持原 `as_of` 重算、保留历史分析 |
| `app/online.py`、`app/pipeline.py`、`app/llm.py` | GNSS 订阅结果进入现有队列与 Qwen 分析，复用同站历史和多站工具 |
| `app/locks.py`、`app/main.py`、`app/config.py` | 共享模型锁、生命周期与维护接口、运行配置 |
| `.env.example`、`scripts/start_server_data.sh`、`.gitattributes` | 服务器配置、在线／回放入口及 Shell 换行 |
| `app/static/index.html`、`app.js`、`styles.css` | 数据获取与存储入口、实际进度、策略与维护、三栏回放看板 |

已实际进行部署、独立环境准备、来源补取、GNSS 解析、Qwen 业务分析、应用重启续接和首轮清理调度。未执行独立测试或 smoke test。专业工具的接线与实际调用分别记录，结果见第 5 节。

## 2. 服务器位置、环境与准确启动方式

| 用途 | `.16` 实际位置或状态 |
|---|---|
| Web 根 | `/home/xnchen/projects/Intelligent-Military-Early-Warning-Agent`；无 `.git`，沿用 tar 包部署 |
| 在线数据 | Web 根下 `.env` 与 `runtime/` 原位置保留，没有迁移或清空 |
| 原件根 | `/home/xnchen/gnss_public_data/chasing_lightning` |
| 共享运行根 | `/home/xnchen/gnss_public_data/early_warning_runtime` |
| 台账／特征 | 运行根下 `catalog/resources.sqlite3`、`results/` |
| 长期证据／交付报告 | 运行根下 `evidence/`、`reports/ROUND5_DELIVERY.md`；Web 根另存同名报告 |
| 回放数据 | 运行根下 `replays/round4/demo.sqlite3` |
| 工作台 Python | Web 根下 `.venv-workbench/bin/python` |
| 模型连接 | `http://127.0.0.1:8000/v1`，实际模型 `qwen3-4b` |
| 在线／回放入口 | `http://10.20.14.16:8080/` ／ `http://10.20.14.16:8081/` |
| 当前进程与管理 | 在线 PID `3899267`，回放 PID `3899268`；沿用 `nohup`，日志为 `runtime/server.log`、`runtime/replay-server.log` |

部署前 Web 使用模型环境 Python，原 PID `3888267` 已正常退出。系统 Python 3.12 缺少 `ensurepip`，本次用现有 Python 3.11 创建独立工作台环境，依赖只安装到新环境。实际首次准备命令如下；日常启动无需重建：

```bash
cd /home/xnchen/projects/Intelligent-Military-Early-Warning-Agent
/home/xnchen/miniconda3/envs/qwen3_4b/bin/python -m venv --clear .venv-workbench
.venv-workbench/bin/python -m pip install ./.deploy/r5-6022da6 -r .deploy/r5-6022da6/requirements-gnss.txt
```

在线和回放的前台启动命令分别为：

```bash
cd /home/xnchen/projects/Intelligent-Military-Early-Warning-Agent
ACQUISITION_AUTOCLEAN=true bash scripts/start_server_data.sh online
```

```bash
cd /home/xnchen/projects/Intelligent-Military-Early-Warning-Agent
DATA_DIR=/home/xnchen/gnss_public_data/early_warning_runtime/replays/round4 ACQUISITION_AUTOCLEAN=true bash scripts/start_server_data.sh replay
```

上述回放目录已保存本轮实例，同目录重启续接。在线默认使用原 Web 根 `runtime/` 和 8080，回放默认使用该独立目录和 8081。沿既有后台管理方式，可分别使用：

```bash
nohup env ACQUISITION_AUTOCLEAN=true bash scripts/start_server_data.sh online >> runtime/server.log 2>&1 < /dev/null &
nohup env DATA_DIR=/home/xnchen/gnss_public_data/early_warning_runtime/replays/round4 ACQUISITION_AUTOCLEAN=true bash scripts/start_server_data.sh replay >> runtime/replay-server.log 2>&1 < /dev/null &
```

脚本不会自动停止已有 PID。本轮先更新并重启在线工作台，再启动回放，使两者使用同一 `catalog/model.lock`；业务数据库隔离，资源与协调器共享。`nohup` 可脱离 SSH 会话运行，没有配置或确认开机自启。本轮未启动、停止或修改模型服务，未向模型环境安装依赖。

## 3. 两例资源与计数口径

部署前于 2026-09-12 12:26:52 UTC，按两份 manifest 精确路径只读文件属性：

| GNSS 需求 | 观测／参考文件数 | 服务器原件存在 | 精确路径缺失 | 字节 |
|---|---:|---:|---:|---:|
| 哈尔科夫 | 32／58 | 90 | 0 | 247,444,624 |
| 霍尔木兹 | 42／12 | 54 | 0 | 52,872,222 |
| 合计 | 74／70 | 144 | 0 | 300,316,846 |

哈尔科夫为 11 个实际站点的标称 30 秒日文件，观测日期 2024-05-08～10，参考为 2024-04-10～12 及 2023-05-08～10；霍尔木兹为清单三站每天 10:00、10:15 的标称 1 秒切片，观测日期 2026-02-27～03-05，参考为 02-11、02-26。名义覆盖与实际有效历元分别保存。

最终 API 与只读 catalog 统计如下，90/54 不作为下载成功目标：

| 案例／资源 | 需求 | 原件复用 | 兼容结果复用 | 真实下载 | 成功处理 | 待处理 | 来源缺失／技术失败 |
|---|---:|---|---|---|---|---|---|
| 哈尔科夫 GNSS | 90 | 90 | 0 | 0 | 90 | 0 | 0／0 |
| 霍尔木兹 GNSS | 54 | 54 | 0 | 0 | 54 | 0 | 0／0 |
| 霍尔木兹 PortWatch 日表 | 35 | 20 | 0 | 15 | 35 | 0 | 0／0 |

按唯一注册资源 ID 计数，GNSS 单位为文件、PortWatch 为日表；复用、下载和成功处理是不同属性，可能重叠，不能相加作为需求数，也不是重试次数。`result_reused=0` 是台账入口标志；实际重启保留并复用已完成计算，未重复解析，该事实不改写为入口结果复用数量。另复用 45 份仓库辅助 JSON：20 份 PortWatch、8 条新闻标题、7 份 GPSJam、10 份 GFZ 日资料，新闻标题未扩写为正文。

## 4. PortWatch 缺日与实际预警

前置目标为 chokepoint6 的 **2026-01-30～02-13，共 15 日**。部署前限定查询 Web `runtime/demo.sqlite3`、霍尔木兹旧报告日表及已知资产根 `external_intel/`，找到 0/15；该结论不覆盖全服务器其他未知目录。仓库已有 02-14～03-05 共 20 日，其中 13 日属于起点前参考。

本轮系统已真实补取前置 15 日，与原有 20 日合并，35/35 日资源完成。首批实际使用 01-30～02-26 的 28 个有效参考日，满足原规则至少 21 日的要求，没有降低阈值或补造缺日。

| 截止日 `as_of` | 源观测日／通行量 | 参考中位数／有效日 | 比例 | 程序实际判定 |
|---|---|---|---:|---|
| 02-28 | 02-27／53 | 70.5／28 | 75.2% | 未触发 |
| 03-01 | 02-28／51 | 70.0／28 | 72.9% | 未触发 |
| 03-02 | 03-01／18 | 69.0／28 | 26.1% | 单日候选 |
| 03-03 | 03-02／4 | 冻结 69.0／28 | 5.8% | 连续两日低于 50%，发布 `active` 异常 |
| 03-06，最终 | 03-05／3 | 冻结 69.0／28 | 4.3478% | `active/ongoing`，未达恢复条件 |

真实异常 ID：`pw_replay-79924b2a-hormuz-portwatch_pw_lowflow_v1_2026-03-01`。首次发布为 2026-09-12 **13:59:21.244661 UTC**；最终证据版本 4，保留 v1～v3 分析历史，当前分析于 **14:29:32.051008 UTC** 完成且未过期。冻结参考区间为 02-01～02-28，共 28 个有效日。Qwen 后续输出 `no_anomaly` 没有解除程序异常，其解释为没有满足恢复条件的新证据。历史来源首次发布时间未知，`available_at=null`，不以下载时间冒充发布时间。

## 5. 两例真实业务结果与依据

所有时间均为 UTC。共享回放实例始终为 **`79924b2ab9334135bf7c3cd8a1b7d92f`**。

| 项目 | 霍尔木兹终态 | 哈尔科夫终态 |
|---|---|---|
| 案例状态／批次 | `completed`，7/7 | `completed`，3/3 |
| 截止时间 | 2026-03-06 00:00 | 2024-05-11 00:00 |
| 实际 GNSS 成功解析 | 54/54 | 90/90 |
| 创建时间，2026-09-12 | 12:58:26.692704 | 12:57:54.190147 |
| 启动时间，2026-09-12 | 12:58:37.916121 | 12:58:27.351958 |
| 完成时间，2026-09-12 | 14:29:33.520362 | 14:49:47.671798 |
| 工作 | 24 条均 `completed` | 12 条均 `completed` |
| 质量与当前缺口 | `assessed`，`runtime_missing=[]`，失败资源 0 | `assessed`，`runtime_missing=[]`，失败资源 0 |
| 实际专业工具调用 | 0，`dynamic_tools_status=not_demonstrated` | 0，`dynamic_tools_status=not_demonstrated` |

霍尔木兹 24 条工作均由 `qwen3-4b` 分析：14 条 GNSS（4 `no_anomaly`、10 `insufficient_evidence`）、7 条 PortWatch（6 `no_anomaly`、1 `candidate`）、3 条新闻（均 `no_anomaly`）。唯一发布异常为第 4 节的 PortWatch 数值规则事件，未发布 GNSS 异常。

该例有 **28 次有保存记录的模型请求**：22 次成功响应、6 次 `invalid_output`；不能写成 28 次成功判断。26 个保存的分析运行中，22 个完成、3 个 `model_failed`、1 个 `interrupted`。失败运行各有原请求和格式修复请求；中断运行没有保存请求记录，不计入 28。一次分析可完成多条工作，因此工作数与运行数不相等。`station_history`、`multistation_check` 已接入，但此次专业工具实际调用为 **0**，未演示动态专业工具调用。

哈尔科夫 12 条工作均为 GNSS，均由 `qwen3-4b` 完成：11 条 `insufficient_evidence`、1 条 `no_anomaly`，未发布异常。13 个保存的分析运行中，12 个完成、1 个中断；12 次有保存记录的模型请求均成功，没有保存的失败响应或格式修复请求，专业工具调用为 0。

可定位的已完成工作示例：前缀 `replay:79924b2ab9334135bf7c3cd8a1b7d92f:hormuz:0:`，`as_of=2026-02-28T00:00:00Z`。

| 工作后缀 | 运行 ID | 完成时间 | 结果 |
|---|---|---|---|
| `gnss:0:v20:gnss_replay_v2` | `25caa8c1c01f40cab515eb402a26311c` | 13:35:42.333740 | `no_anomaly` |
| `gnss:1:v20:gnss_replay_v2` | `c598cbc75a0b4975803139a68c21faca` | 13:35:48.543717 | `insufficient_evidence` |
| `portwatch:0:v20:gnss_replay_v2` | `5f2191fef3e74ff3bb12fd5bcfef5698` | 13:36:11.763703 | 程序未触发，Qwen `no_anomaly` |

哈尔科夫代表工作为 `replay:79924b2ab9334135bf7c3cd8a1b7d92f:kharkiv:2:gnss:3:v61:gnss_replay_v2`，运行 `2b078921482545ca8c05236ebe7a59ca`，于 **14:49:46.086957 UTC** 完成，截止为 2024-05-11 00:00。

完整工作、请求、响应、错误、工具和异常历史保存于 `replays/round4/demo.sqlite3`，通过 `/api/work/{work_id}` 和运行详情按需读取。

真实 GNSS 结果包括：

| 原件 | 实际采样与 UTC 覆盖 | 保存结果 |
|---|---|---|
| `GLSV00UKR_R_20241290000_01D_30S_MO.crx.gz`，哈尔科夫观测 | 30 秒；2024-05-08 03:14:42～21:01:42 | 2,065 历元、20 种信号、4,300 个五分钟信号窗口；69 缺历元、1 段缺口，非完整全天 |
| `BSHM00ISR_R_20260571015_15M_01S_MO.crx.gz`，霍尔木兹参考 | 1 秒；2026-02-26 10:14:42～10:29:42 | 900 历元、26 种信号、104 个五分钟信号窗口；无缺历元、无缺口 |

两份结果记录 gzip/Hatanaka 解码、RINEX 3.04、GPS 时制及 `UTC=GPS−18s`，dB-Hz 单位来自 `SIGNAL STRENGTH UNIT`；时制记录引用 IERS Bulletin C72，不主张亚秒同步。对应结果位于运行根 `results/`：

- `rinex-cnr-window-v1-58fffebbc8fd56ebacaeb5e32f91c0f1179d6187a25c1cd12960d31af08e969b-9b83cb8aa275020b.json`
- `rinex-cnr-window-v1-86ae9a74ebc25979316e2947623e42dda91b0ad7417afef8414a895c54b8d58c-f5f1fdcbcf6d168f.json`

解析成功与 CNR 参考不足分开记录。例如 NICO 第三批原件已处理 900 历元、17 种信号，C:S1P／S2I／S5P 的实际强度 p10 为 41／41／42，但文件未明确 dB-Hz，保存为 `receiver_units`，`cnr_p10=null`；4 份前置参考同样如此。这不是缺原件或解析失败。同批 KITG 有真实 dB-Hz 值和 4 个匹配参考窗。已补模型输入的实际状态、原始强度和单位依据；旧材料“待解析”说明属于早期输入，既有完成的模型输出原样保留。

哈尔科夫代表工作中，模型称观测不足，但保存的实际 `attempt.request.messages` 正文包含已计算的 GANP、JOZE，各 2,880 历元、各 6 份可用参考。GANP C:S1X 的实际 CNR p10 为 37.1 dB-Hz，参考窗口中位数 37.49、窗口差值中位数 −0.17；初始信号摘要、其余信号列表与可补查说明均已发送。因此 `insufficient_evidence` 是模型对输入的判断，部分解释仍有局限，没有证据表明这些字段在传输时丢失，也不能据此断言全部数据缺失或解析失败。`assessed` 表示流程得到分析结果，不等于这些模型解释已经证实正确。

## 6. 已观察的资源到达、续接与修复

PortWatch 前置 15 日实际到达，GNSS 参考和观测逐份保存，原实例从等待资源推进至分析。霍尔木兹参考由 33 项辅助资料补到 45 项，实际 PortWatch 前置基线形成 28 个有效日。真实输入变化产生版本（如首批 `v20`），原 `as_of`、工作和历史证据保留；下载时间变化、轮询和缓存清理不制造新分析版本。

正常业务中出现的错误已就地修复并续跑：

- Shell CRLF、PortWatch 格式／已有异常标识冲突；第五批原工作于 **14:17:45.385809 UTC** 成功续跑，保留此前 3 次尝试历史。
- GeoRinex 改为 128 个原生历元块，不降采样；处理 `SYS/PHASE SHIFT` 与 `GLONASS SLOT / FRQ #` 头更新，保留实际观测。GLSV 2024102 于 **14:08:25.252980** 重试成功，版本 `2ce91c15a18547ddbc061faeb68b1cc9`；MDVJ 2024102 于 **14:22:02.302749** 成功，版本 `9e15490030414a91a2ca6b7cd9f81b59`、2 次计算尝试、错误清空。
- 公平轮转各案例参考与最早观测；PortWatch 小 JSON 独立导入；同站参考范围读取与逐资源让出执行机会，减少等待。
- 未来资源只更新可用性，完成批次由真实工作派生；模型锁内刷新工作，跳过新版已替代的输入。线程阻塞 `flock` 共享模型串行入口，不宣称严格 FIFO。

重启始终沿用同一实例和数据目录，原件、缓存、失败历史与已完成结果保留。没有删清单、换站点、降低参考要求、重建空实例或注入故障来制造完成状态。

## 7. 存储、保留与实际清理

持久策略现为 `autoclean=true`，按用户对本项目本用户范围的持续授权启用；数据库已存策略优先于首次初始化环境默认值。144 个旧 GNSS 原件仍为 `managed=false`，不会自动接管清理。14:51:26 UTC 的存储接口记录如下，已登记数据范围为 2023-05-08～2026-03-06：

| 分项 | 字节 | 口径 |
|---|---:|---|
| 受管原件 | 11,592 | 新补取的 PortWatch 日表 |
| 未接管原件 | 300,337,060 | 144 个 GNSS 原件 300,316,846，加旧 PortWatch 日表 20,214 |
| 临时／预留 | 0／0 | 当前台账 |
| 完整特征 | 238,452,303 | 当前 catalog 已保存结果 |
| 证据 | 1,360,900 | 最近清理记账 |
| 报告 | 0 | 本报告尚未落位时的快照 |
| 数据库 | 74,252,288 | 最近清理记账 |
| 受管额度 | 53,687,091,200 | 50 GiB |
| 磁盘可用 | 102,503,288,832 | 接口读取时值 |

原件与特征来自当前 catalog；证据、报告、数据库分项来自 14:07 的最近清理记账，不是实时全盘审计。报告落位后报告空间不再为此快照的 0。

| 内容 | 当前默认保留与处理时点 |
|---|---|
| 停止任务残片 | 最后写入 24 小时后，每小时回收；活动任务保留 |
| 解压文件和中间数组 | 当前处理结束后释放 |
| 成功计算压缩原件 | 结果保存后 72 小时，每小时处理 |
| 异常关联原件 | 计算完成／首次关联较晚时刻起 7 天 |
| 未处理或失败原件 | 最多 7 天，先保留缺口与重取依据 |
| 完整五分钟特征 | 90 天；活动参考和未完成任务所需结果继续保留 |
| 普通日志、未引用冗长响应 | 30 天；每日 Asia/Shanghai 03:30 与完整特征维护 |
| 日汇总、参考摘要、日表、报告、异常历史、引用窗口和关键工具依据 | 长期保留 |

受管新原件与临时区额度 50 GiB，高水位 45 GiB，回收目标 35 GiB；证据、数据库、报告与未接管旧原件单列，不属于这 50 GiB。活动消费者需要的文件及结果未保存的原件保留；空间不足时等待缓存空间，不删除证据或在线数据库。

首轮自动清理于 **2026-09-12 14:07:26.641629 UTC** 实际执行：0 个文件、释放 0 字节、错误为空，没有符合条件的到期文件。下次小时任务记录为 15:07:22.637406 UTC，日任务为 2026-09-13 03:30（Asia/Shanghai）。尚未观察到 72 小时或 7 天自然到期删除，未改时钟或造旧时间。原件清理后保留来源身份、清理记录、结果、参考、必要曲线和报告，需要原件时可提交服务器重取任务。

## 8. 在线获取与前端

在线持续 GNSS 已接订阅发现、获取、计算、工作去重和 `assess_update()`：高频默认 15 分钟、日文件 1 小时，日期、时段和实际站点由保存订阅决定；保留扫描位置、完整覆盖位置和补缺队列。未定义新的阈值或将历史案例参考套用到当前观测。**实际订阅表为空，本轮没有配置长期 GNSS 站点，也没有该订阅持续发现／获取／计算／分析的运行记录。**

既有在线新闻与 PortWatch 已实际自动运行。在线工作 `numeric:civil-hormuz:pipeline-portwatch:1:shipping_v1` 于 **13:09:08.645315 UTC** 完成真实 Qwen 分析；其原错误修复后回同一队列，保留尝试历史。这是在线业务证据，不计入历史两例。最终在线快照为 241 条工作完成、33 条等待重试；33 条均为新闻，错误为模型在一次格式修复后仍无法解析输出。RSS 正常周期返回无新资料，PortWatch 正常按 24 小时周期推进。

GDELT 为部分覆盖，具体错误 `HTTP 429 Too Many Requests`，已尝试 6 次；最近成功为 13:00:35、最近检查为 14:31:12、下次退避重试为 15:03:13 UTC。保留自动重试，不将在线错误与已经完成的双案例混算，也不宣称全部在线任务无错或长期稳定性已经得到证明。

“数据获取与存储”入口显示订阅、两例需求、复用／下载／计算分项、实际字节、存储路径和保留策略。只在打开入口时轻量轮询摘要，详情按需读取；未知总量显示未知。维护操作提交服务器任务，浏览器不承担下载、解析和正常调度。

三栏在线／回放看板保留；案例覆盖改为当前 `resource_counts`、`baseline_status`、`runtime_missing`，不把 manifest 旧笔记本缺失盘点混成当前缺口。`ea2915a` 静态文件已直接部署，刷新页面即可加载，不需应用重启。

**本轮没有取得实际页面截图。** CUA 返回 `No browser is available`，表面清单为 `apps=[]、browsers=[]`；Codex 打开页面排队后再读取一次仍不可用，故未继续循环。这不能证明页面正常或异常，也没有以接口结果或静态图代替截图。

## 9. 剩余项

两例业务均已完成，没有未处理或失败资源。浏览器连接不可用，实际页面截图尚缺；需要可用的浏览器连接才能补齐页面取证。在线 GDELT 的来源限流与 33 条新闻格式错误保留原队列自动退避，未扩展为新的新闻修复任务。长期 GNSS 订阅尚未配置，缓存自然到期尚未发生；不以历史回放或首轮零删除代替。

当前 GNSS 原件均已在服务器找到，无需用户从笔记本手工搬运。长期 GNSS 获取的最小配置条件是明确实际站点、来源、产品和时间范围后保存订阅；本轮没有擅自建立长期订阅。本轮交付不扩展新案例或新算法。
