# Round 6 双案例运行报告与下一轮重点问题

**哈尔科夫与霍尔木兹两个案例仍未完全通过。当前最重要的缺口是：系统还没有证明自己能在事件发生前，依据当时可得的资料给出有依据的预警。** 已完成的采集、回放、工具调用和页面展示是工程成果，不能代替这个业务目标。

本报告合并了 2026-09-14 用户最新要求，供 GPT 规划下一轮。开头是下一轮必须优先回答的问题；后文保留 Round 6 的实际运行依据。此次补充没有产生新的分析结果，原始统计与时间线仍对应 Round 6 保存的运行。

## 给 GPT：下一轮首先解决事前预警，而不是事后解释

### 用户的强需求

这两个历史案例必须作为后续工作的必做案例，最终按完整业务目标验收。用户要求把重点放在 LLM 判断逻辑和分析方法是否真正有效：能否从事件前的资料中形成明确、可解释的信号。不能继续用“任务跑完了”“LLM 调用了工具”“地图已经标红”宣称案例通过，也不能把修好文案当成解决分析能力。

**用户进一步明确：哈尔科夫和霍尔木兹的最终验收目标一致，都是事件发生前、有明确证据支持的红色预警。黄色“可疑、继续观察”只算调查过程中的中间状态，不算案例最终通过。** 哈尔科夫当前的黄色结果尚未达到这一目标；霍尔木兹事件之后才出现的红色也尚未达到。请 GPT 将“事前明确红色”列为两例各自需要解决并用真实运行结果证明的重点要求。

红色必须由当时可得的证据和分析结果支持，不能把现有黄色直接换成红色，也不能按已知历史结局倒推判断。证据和方法仍不足以支持时，应明确记录目标尚未完成及具体原因，继续针对实际问题规划调整，不能把黄色包装为红色验收通过。

用户提出应考虑调整 LLM 判断逻辑，或引入更有效的分析方法。这里要解决的是数据如何形成判断；只在前端增加算法、调颜色或改标题，不能改变证据出现得太晚的问题。请 GPT 先明确现有方法到底缺在哪一环，再规划相应调整，不要又以整理输入、增加工具调用次数或修复输出格式作为最终交付目标。

### 霍尔木兹为什么没有通过

本报告采用的事件起日是 **2026-02-28**。目前航运黄色来自 **03-01 日观测，在 03-02 回放时刻形成**；红色来自 **03-02 日观测，在 03-03 回放时刻形成**。两者都在事件之后。

**03-01 至 03-08 的航运下降不能证明对 02-28 事件的提前预警。** 这段数据可以说明事后的航运变化和持续时间，但不应再作为下一轮预警能力的主要成功证据。继续精修这段数据的解释，不能补上事件前没有形成有效判断的缺口。

下一轮首先要回答：02-28 之前，已有资料及当时可得的来源版本究竟支持什么判断？目前已保存的前期资料是否被 LLM 正确理解？现有方法是在识别有价值的变化，还是只能在明显下降之后做解释？请把答案与证据日期、实际可得日期、结论形成时间对应起来。

### 哈尔科夫为什么也没有通过

目前首次黄色的回放时刻是 **2024-05-10 00:00 UTC**，事件注释也只核实到 **05-10 这一天**。这只能说明处于同一日历日期，尚不能证明提前。已有黄色对应实际接收站的部分历史偏离；多站变化关系、环境因素解释与资料当时是否可得仍未说清楚。

下一轮须分别回答：这些已保存的变化具体支持什么结论，模型实际使用了哪些证据，哪些已返回的事实没有被解释，以及相关判断在历史上最早何时可能形成。不能把单个窗口出现黄色直接等同于整个案例预警成功。

### 请 GPT 按这些问题规划

| 必须回答的问题 | 希望拿到的明确结果 |
|---|---|
| 事前证据是否成立 | 每例分别列出事件前已有的观测、来源版本和可得时间；缺少哪项时间依据就直接写明 |
| LLM 到底错在哪 | 区分没读到数据、读错统计、未使用工具返回、未解释相反证据，以及资料本身不足；用现有工作和实际返回说明 |
| 需要调整哪一层 | 分清输入组织、LLM 判断与解释、数据分析方法、资料覆盖或到达时效的问题；提出与真实断点对应的调整理由，不能只换提示词口气 |
| 怎样判断 LLM 的实际贡献 | 明确某条结论由数值规则直接产生，还是模型读取补充证据后改变了判断；不能只用请求数和工具次数证明智能分析有效 |
| 怎样验收两例 | 两例的目标均为事件前、有明确证据支持的红色预警；分别列出首黄、首红、形成时刻与对应依据。黄色中间状态或事后红色均不算最终通过；工程完成与预警通过分开填写 |

已知历史结局用来评价结果，不能送回当时的判断，也不能按结局倒调阈值或强行标红。如果资料和方法仍不能支持事前判断，就明确写出哪一例尚未通过、卡在哪里、最直接要补什么；不能伪造成功，也不能用笼统的“证据不足”结束分析。

下一轮仍须沿用已经确定的“说人话”要求：主卡先写结论，再列对象、往日数值、本次数值和时间；详细分析与来源按需展开。当前前端已在 `d75723f` 改为这种表达，早先截图中的流程话已经移除。**表达改进已经完成这一轮修改，下一轮的首要问题是事前证据和判断能力。**

以下为 Round 6 的实际执行记录，不应读作两例已经通过预警验收。

## 部署与边界

- 页面：<http://10.20.14.16:8081/static/replay-map.html?ui=round6>。原历史页面路径继续使用同一份页面；浏览器查看不触发模型或工具。
- 8081 专用代码：`/home/xnchen/projects/Intelligent-Military-Early-Warning-Agent-round6`。
- 固定结果目录：`/home/xnchen/gnss_public_data/early_warning_runtime/replays/round6`；本轮未通过新建空库绕过失败。
- 实例：`fa414cc4276140ad9e3c2cb7981dd312`；分析版本：`round6_observation_v1`。
- 8080 保留原代码目录、数据和进程。历次必要部署记录中的 8080 PID 均为 `3899267`，未重启、迁移或覆盖其文件。
- Qwen3-4B 使用原 `127.0.0.1:8000/v1` 服务和模型锁；未升级或重启模型环境。
- 保留 R5 实例 `79924b2ab9334135bf7c3cd8a1b7d92f` 与 `replays/round4/demo.sqlite3`。原件、兼容特征、资源台账与清理策略继续共享。

## 实际结果

| 验收层 | 哈尔科夫 | 霍尔木兹 |
|---|---|---|
| 工程执行闭环 | 3/3 批完成；获取/复用、计算、Qwen、保存、页面与真实故障续接已完成 | 36/36 批完成；上述环节已完成 |
| 证据与自主核查 | 已演示真实工具返回进入同一判断；多站差异与环境解释仍不完整，部分完成 | 已演示同站、多站和历史航运资料核查；三条 GNSS 解释错误及其他未解项保留，部分完成 |
| 观测异常业务 | 接收站有限历史偏离支持黄色核查线索；不支持城市级异常或 GNSS 红色/绿色判断 | 既有海峡低通行量规则支持红色，至 03-08 观测仍未恢复；GNSS 无足够依据出具当前三色判断 |
| 历史事前可得性 | 未验证，严格提前量无法核定 | 未验证，严格提前量无法核定 |
| 原热冲突预警目标 | 尚未通过：没有证实事前判断成立 | 尚未通过：当前航运首黄、首红均发生在事件之后 |

哈尔科夫首次有效黄色为 `as_of=2024-05-10T00:00Z`，后续保持至 05-11；仅按日期与 05-10 事件注释为同期关系，不能断言提前。霍尔木兹航运首次黄色为 `as_of=2026-03-02T00:00Z`（03-01 日观测），首次红色为 `as_of=03-03T00:00Z`（03-02 日观测），与 R5 未改规则一致；按日历均晚于 02-28 事件注释。最后 `as_of=03-09T00:00Z` 的 03-08 日通行量为 2 艘次，冻结参考 69 艘次，仍未满足连续两日达到 80% 的恢复条件。

两例共用现有地图主卡与报告组件，颜色由后端明确的观测对象决定。实机主卡与侧栏来自同一快照，普通页面无队列、重试、工作 ID 或请求日志。每例主地图与展开报告均有真实截图及后端快照引用。

| 执行统计 | 哈尔科夫 | 霍尔木兹 |
|---|---:|---:|
| 登记 / 已导入资源 | 93 / 93 | 120 / 120 |
| GNSS 原件及既有特征复用 | 90 | 54 |
| 当前判断工作完成 / 未完成 | 3 / 0 | 28 / 0 |
| 实际模型选择 / 工具执行 / 复用返回 | 11 / 10 / 1 | 47 / 42 / 5 |
| 程序必做计算项目 | 6 | 56 |
| 有具体问题、真实返回及最终引用的调查链 | 4 | 33 |
| 所有版本保留的实际模型请求 | 74 | 373 |
| 所有版本保留的真实工具执行 | 15 | 103 |

以上来自最终导出的 `input_execution` 与 `autonomous_investigation`。模型请求包含真实故障、定向修订和后续被替代的尝试；没有只统计成功末次。当前工具统计也保留同一有效工作故障续接前的实际调用。程序计算按对象、可见截止、证据版本去重，不是底层函数调用次数。哈尔科夫 3 条、霍尔木兹 28 条工作完成表示工作生命周期结束，其中三条 GNSS 解释仍未通过；资源缺失、失败与待处理均为 0。

## 本轮解决的具体问题

1. **已有统计被解释成没有资料。** R5 哈尔科夫请求已包含 GANP、JOZE 观测与参考。总体 p10、参考窗口中位数和逐窗差值中位数的口径不同，不能直接相减诊断程序错误。本轮保留完整信号目录、有效样本、单位、参考日期组和有选择依据的片段，提供真实窗口工具。
2. **单位和覆盖限制被混同缺文件。** NICO 的部分原件使用 `receiver_units`，解析成功但不能作为 dB-Hz 比较；不补零、不推断单位。实际站点不代表哈尔科夫市区或霍尔木兹海峡的直接覆盖。
3. **多个文件组与浏览器各自拼结论。** 判断改为案例、风险对象、可见截止、证据版本的持久记录。后端选定主卡对象，保存简述、证据、时间、调查和修订；前端不再由负差、`no_anomaly` 或最大颜色猜状态。
4. **高频资料被人为延迟到次日零点。** 复用真实释放窗口；日文件仍在整份文件可得后使用。霍尔木兹航运从 02-23 起、GNSS 从 02-27 起独立配置，02-26 GNSS 仅作为其独立起点前参考。
5. **实际模型请求超长及输出约束冲突。** 原请求超过现有 8192 token 上限。改为紧凑目录，保留全部信号、日期和材料关系，工具详情替换初始片段；共享窗口、单位和日期集中列示。航运首次候选重复携带参考日表时，保留完整数值并改用紧凑表。真实返回又暴露出本应用自定义文本正则吞入 JSON 结构、禁止数字导致重复词和信号代码变形等问题。最终移除这类文本正则，使用原服务原生结构化输出与有效字符串长度约束，并对齐决策、缺口和历史事实引用要求。格式修复不再追加整段错误回答。失败响应、尝试与续接均保留。
6. **提出问题却不调查。** 实际记录出现 `no_anomaly` 绕过未解问题，以及只有 `resolved=true`、没有解决依据的情况。另一次多站调用虽成功，但因省略信号和时间参数，实际查询与提出的问题不同；工具执行成功不能直接当作问题已解决。修复的是证据求证流程，不提高异常积极性。程序必做计算与模型选择的真实工具分开计数。
7. **实际注册和页面响应故障。** 航运起点前移后重划已归档资料角色；注册恢复后保留原失败原因并继续。较重的纯 GNSS 统计移出异步请求线程，保留原 SQLite 写入路径。
8. **失败影响了无关工作。** 移除单个失败对同例其他排队修订的级联取消。已明确的上下文输入错误保留本工作重试，不再作为模型服务故障阻塞健康的另一条路径。
9. **实机报告遮住主卡。** 报告展开时按侧栏实际宽度平移地图，使主卡完整可见；连接恢复后清除旧提示。保留地图、图层、三色标题和按需侧栏的原布局。
10. **模型重抄统计时错列、泛化或复制占位文字。** 实际结果曾把历史前日数值写到后日，把油轮数量写成总船次，并将单站结果泛化为其他站均正常。现在由模型选择已读事实引用并解释有限意义；对应日期、字段、窗口、参考与数值由程序按真实引用呈现。黄色仍须有专业工具返回和模型支持，不能由负差、事实存在或模板字段非空触发。
11. **错误解释与真实恢复混淆。** 已保存的解释通过定向修订纠正；保留旧工作、模型返回和告警版本，当前地图与报告一起更新。旧 GNSS 支持不成立时记录为解释修订撤销，不冒充物理观测恢复。已成立的航运数值规则则另行处理：3 月 8 日解释使用原告警版本 6，3 月 9 日当前版本 7 保持，修订只影响对应分析文字，不重算规则、倒退当前版本或解除红色提醒。
12. **环境归档只在目录中列名。** 对原目录中当时已释放的 GFZ 归档，补入实际 Kp、ap、Ap、太阳射电指标、日期、单位和引用；保留旧输入修订。哈尔科夫 `as_of=2024-05-10` 的四次真实请求已包含 05-08、05-09 指标，但最终论证只引用 GNSS 工具，不能写成已经排除自然因素。霍尔木兹 `as_of=2026-02-27T10:30Z` 当时没有已释放环境产品；其最早 02-27 日产品在模拟 02-28 零点释放，未用未来资料补齐过去解释。

**一次有依据的最小预算调整。** 原起点为每次分析 4 次请求、2 次工具，每请求最多 1800 输出 tokens、上下文 8192。3 月 1 日两条 GNSS 的真实记录均为：初判提出问题 → 同站工具 → 同期多站工具 → 读取两次返回后作最终判断；四次没有格式重试或重复工具，事实矛盾恰在第四次出现，无法执行工单要求的一次纠正。按工单 4.3，仅为这类必要步骤已占满预算、此前未作格式或事实纠正的 GNSS 判断，保留第 5 次仅作最终回答的事实纠正机会。工具数、上下文和输出上限不变，不扩充一般重试；第五次仍错误则保存解释问题。实际是否使用该机会及原因随请求保存，不能把新增机会写成修正必然成功。

## 数据范围与解释

哈尔科夫首先使用原有 90 份 GNSS 文件，观测日期 2024-05-08～10。2023 年同季与 2024 年 4 月的参考分组展示，不合并成稳定总体。有限样本范围只能支持描述性核查，不能解释为显著性概率。本轮没有新增 GNSS 红色或绿色阈值。

现有三日包含连续的日文件边界，七个可比站贯穿三日，BUCU 只在后两日进入相应比较。本轮未启用第二阶段 GNSS 扩窗：实际断点是有限参考、多站解释和空间代表性，尚未发现必须补前后日期才能解释当前有限结论的连续时间缺口。最新 BUCU 首末窗口支持黄色核查线索，但全日 289 个窗口的变化并非统一下降；不能把首个仅 18 秒、1 个有效历元的边界窗口当成全天持续异常。有效比例按卫星集合计算，也不是时间覆盖率。哈尔科夫本例未配置航运来源，其航运空状态不计为应下载资料缺失。

霍尔木兹保留原有 54 份 GNSS 文件。PortWatch 观测扩展到 2026-02-23～03-08，28 日参考从 01-26 起；优先复用原有 01-30～03-05 日表，仅补取 01-26～29、03-06～08。原 `pw_lowflow_v1` 不变：至少 21 个有效参考日，连续两日低于冻结参考 50% 触发，连续两日达到 80% 恢复。

另取得原研究文件事先登记的两段历史窗口原始日值：2020-10-03→04、2019-11-01→02，共四日，作为有限船流构成参考。只读这四日，未导入旧研究结论或后续事件标签。两段既已作为参考，就不作为独立验证，不据此报告准确率或误报率。名义运力及单位船次名义运力不等于实载货量。WTO 未因名称相关而额外接入。

前期构成对照中，02-24→25 总船次 42→112、油轮 20→57、货船 22→55，单位船次名义运力变化约 −0.60%；这支持船次增加的描述，不能写成平均单船名义运力上升。02-25→26 总船次 112→86、名义总运力 5,545,881→1,823,647、单位船次名义运力约 49,516.79→21,205.20，属于数量和构成同时变化的描述性线索，没有产生新红色规则。2019 年参考总船次 69→67，而单位船次名义运力下降约 55.54%，说明数量与构成指标不能互相代替；两日对照不证明季节性、操作原因或本次变化属于正常分布。

后期的小基数尤其需要单独解释：03-04→05 总船次从 0 增为 3，可以报告绝对增加，不能计算相对百分比；程序展示已由原值修正，模型原回答保留。03-06→07 名义总运力 143→102,414 的大百分比不代表绝对水平高于历史两日 3,044,613 与 1,314,529，也不代表低通行量已经恢复。GNSS 的 03-02 工具选择为 C:S5P，其他多日为 C:S1P，不能直接拼接成同一信号的连续趋势；同一时段 BSHM 与 KITG 也常呈不同方向。没有取得同期自然因素和设备依据时保持具体解释缺口。

144 份原 GNSS 原件均在服务器复用，不重新下载、不转入自动清理对象。新下载通过既有受管资源和缓存处理；完整模型及工具记录留在本轮数据库，交付仅保留必要业务结果与截图。

本轮台账中的新下载共 11 份、8,421 字节，新结果版本同为 11 份、8,421 字节；未取得本轮起点的完整占用快照，不推算全盘净增长。共享持久清理策略仍为 `autoclean=true`：成功计算受管原件 72 小时，异常关联与失败原件 7 天，残片 24 小时，完整特征 90 天，普通日志 30 天；必要引用窗口、日表、参考摘要与报告长期保留。原件清理每小时，日维护为北京时间 03:30；50 GiB 受管额度、高水位 45 GiB、回收目标 35 GiB 均未调整。2026-09-13 16:30:10 UTC 的实际清理记录为 0 项、释放 0 字节，不能据此宣称已观察到全部保留期自然到期。

2026-09-13 18:22:30 UTC 的本轮已知文件占用为：数据库 278,798,336 字节（约 265.9 MiB），服务器日志 90,734 字节，部署记录 6,593 字节。数据库包含保留的实际分析、工具、失败和解释修订，不等于新增原件容量；以上为当前占用，未推算全服务器净增长。

## 尚未完成的具体项

| 未解项 | 已有依据与本轮状态 | 最直接后续动作 |
|---|---|---|
| **最高优先级：事前预警未成立** | 霍尔木兹 03-01～08 航运变化晚于 02-28 事件；哈尔科夫首黄与事件只有同日关系。两例均未证明当时可得资料能支持事前判断 | 按本报告开头的问题规划下一轮，先核定事前证据及 LLM、分析方法的真实断点；事后下降不能充当事前预警成绩 |
| GNSS 完整解释 | 同站、多站工具实际返回，但部分判断未解释相反方向、信号切换与环境作用。霍尔木兹 03-03 10:30、03-05 10:15、03-05 10:30 三条当前结果仍为 `analysis_explanation_issue`，不据其发布无支持的判断 | 使用保存的同窗和参考值解决对应事实矛盾及完整解释；保留原失败，不重下原件或无限追加模型请求 |
| 自然因素与设备原因 | 哈尔科夫最终输入允许引用已归档 GFZ；模型没有在最终论证采用，不能称自然因素已排除。霍尔木兹高频截止常只有前一日环境资料，接收机记录缺失 | 先说明已归档指标对当前解释的支持范围；设备及同期环境证据确实缺失时保留未解 |
| 航运构成解释 | 03-08 修订准确引用本次总船次 2→3、历史 69→67，但未完成原名义运力问题；“未达到显著异常水平”无校准阈值或检验依据 | 从已保存的同字段名义运力解释绝对水平与小基数变化；该解释不得解除当前低通行量规则红色 |
| GNSS 判定范围 | 有限历史样本、远端站点，未形成经确认的 GNSS 红色/绿色业务阈值，也不直接覆盖目标城市或海峡 | 在本轮实际站点范围内明确比较成立条件，另行形成可验证的业务阈值依据 |
| 历史事前可得性 | 主要历史资料首次发布时间未知；模拟释放已分开保存。事件只有日期精度 | 找到来源历史版本及首次可得证据后，才评价严格事前关系；当前严格提前量保持空值 |

最后两条定向修订（02-27 10:15、03-04 10:30）已在唯一条件性第五次纠正后消除范围事实矛盾，保持有限的“证据不足”判断；这并未解决完整多站、环境和原因解释。其余第五次仍不一致的结果按问题状态保留。本轮没有通过继续放宽预算将所有模型返回强行改为通过。

## 时间评价

分别保留观测时间、来源可得时间、系统取得、模拟释放、实际分析和发布时间。没有历史首次可得依据时维持空值，不能用当前下载时间或模拟释放推导真实提前量。

历史注释单独保存在 `cases/round6/evaluation_annotations.json`，不进入特征、模型、工具候选或状态规则。[OHCHR 2024 年 5 月报告](https://ukraine.ohchr.org/en/protection-civilians-armed-conflict-may-2024)核实到哈尔科夫新一轮地面攻势起日 2024-05-10；[联合国报道](https://turkiye.un.org/en/310903-bombing-iran-and-retaliatory-strikes-%E2%80%98-grave-threat-international-peace-and-security%E2%80%99)核实到美以对伊朗打击与后续反击的日期 2026-02-28。两者均只有日期精度，事件时区、精确时刻未核定。严格提前小时数保留空值。

R5 原规则对照是：03-01 观测在 `as_of=03-02` 成为候选，03-02 观测在 `as_of=03-03` 满足连续条件。告警记录起日不能替代首次满足条件的回放时刻。本轮最终日历关系从保存时间线报告，不人为对齐旧表。

热冲突事前预警能力单独保持未验证。本轮交付历史 GNSS 观测质量和民用航运异常，不用观测异常闭环代替军事预测能力验收。

## 准确启动与续接

8081 正在运行时不重复启动。服务已停止时，在 `.16` 执行：

```bash
cd /home/xnchen/projects/Intelligent-Military-Early-Warning-Agent-round6
nohup bash scripts/start_round6.sh \
  >> /home/xnchen/gnss_public_data/early_warning_runtime/replays/round6/replay-server.log \
  2>&1 < /dev/null &
```

该入口复用原 `.venv-workbench` 解释器及原项目配置中的连接参数，固定到本轮代码、8081 和结果目录。重启后恢复同一实例；原排队、重试和已完成依据继续保留。`nohup` 只实现脱离 SSH 运行，本轮未配置服务器开机自启，也不以历史回放证明长期订阅能力。

仅在真实故障修复、且对应工作已经失败时，可续接原工作：

```bash
curl -X POST http://10.20.14.16:8081/api/replay/cases/kharkiv/resume \
  -H 'Content-Type: application/json' \
  -d '{"reason":"说明已修复的实际故障"}'
```

已处于等待重试的工作会按原记录自动重试。因真实解释逻辑修复而需要重判某条已完成结果时，使用同一入口的 `work_ids` 明确列出受影响工作并提供原因；保存解释修订副本、原请求与旧快照，不清空案例或重跑无关工作。

## 交付依据

最终 `ROUND6_CASE_RESULTS.json`、每例业务报告及时间线由 `scripts/export_round6_results.py` 从同一个 SQLite 读取快照生成。前端实际截图另记录对应的后端快照 ID。页面读取固定快照，报告打开期间有新结论时提示整体更新，避免新卡片拼旧报告。

本报告是主入口；以下配套文件均在仓库 `reports/round6/`：

- `ROUND6_CASE_RESULTS.json`：统一机器可读验收结果。
- `ROUND6_KHARKIV_REPORT.md`、`ROUND6_HORMUZ_REPORT.md`：自动生成的业务报告。
- `ROUND6_KHARKIV_TIMELINE.json`、`ROUND6_HORMUZ_TIMELINE.json`：按 `as_of` 排序的状态与证据。
- `ROUND6_KHARKIV_MAP.png`、`ROUND6_KHARKIV_REPORT.png`、`ROUND6_HORMUZ_MAP.png`、`ROUND6_HORMUZ_REPORT.png`：实际主地图和展开报告截图。
- `ROUND6_FRONTEND_EVIDENCE.json`：截图对应快照、真实续接记录，以及带当前工作/运行引用的交付复核未解项。

服务器报告位于本轮固定结果目录下的 `reports/`；完整原始模型与工具记录仍保存在 `demo.sqlite3`。页面截图时间为 09-13 UTC，分别对应哈尔科夫资料截止 2024-05-11 和霍尔木兹资料截止 2026-03-09，不将截图执行日期当作历史资料可得日期。

实际页面记录保存后，可从本轮数据库重新导出业务报告：

```bash
cd /home/xnchen/projects/Intelligent-Military-Early-Warning-Agent-round6
/home/xnchen/projects/Intelligent-Military-Early-Warning-Agent/.venv-workbench/bin/python \
  scripts/export_round6_results.py \
  --data-dir /home/xnchen/gnss_public_data/early_warning_runtime/replays/round6 \
  --catalog-db /home/xnchen/gnss_public_data/early_warning_runtime/catalog/resources.sqlite3 \
  --output-dir /home/xnchen/gnss_public_data/early_warning_runtime/replays/round6/reports \
  --run-started-at 2026-09-13T13:58:54Z \
  --frontend-evidence /home/xnchen/gnss_public_data/early_warning_runtime/replays/round6/reports/ROUND6_FRONTEND_EVIDENCE.json
```

导出只读取既有结果，不重新分析或下载。原件复用、已消费的既有兼容特征、新下载以及模型自主工具分别计数。解释修订复用原数值，不重复计为程序计算；所有版本的真实工具执行另列，避免只留下最终成功次数。

本轮执行真实资料获取、既有特征读取、必要计算、Qwen 分析、修复续接与实际页面查看。未运行独立 smoke test、单元测试、压力测试、故障注入或端口扫描。
