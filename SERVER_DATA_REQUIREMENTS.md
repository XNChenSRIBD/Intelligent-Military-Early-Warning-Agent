# .16 常驻数据获取、下载管理与缓存需求

日期：2026-09-12。针对第四轮提交 `24772bb` 补充，作为下一步实施依据。本文明确目标行为和默认参数；下载管理页面、GNSS 持续采集接线及下述清理策略尚未实现或部署。

## 1. 运行职责与当前缺口

`.16` 是常驻采集、存储、GNSS 计算、Qwen 分析和发布节点。笔记本负责开发、浏览和配置，不承担生产原始数据存储，也不作为下载中转。关闭浏览器、笔记本断网或关机后，服务器仍按已保存配置工作；再次打开页面读取服务器保存的进度和结果。

第三轮已实现新闻和 PortWatch 在线采集的代码；第四轮 GNSS 入口目前依赖指定目录中的原文件，缺失时记录 `missing`。它尚未把缺文件转换成下载任务，也没有把持续到达的 GNSS 文件接入在线队列。现有前端尚无统一下载管理和缓存管理。因此第四轮不是完整的服务器持续监测交付，不能把用户手动备齐本机文件作为最终解决方式。

此前“本地未找到 144 个原文件”仅指笔记本资产目录的状态。服务器现存文件应优先复用；只有服务器也没有且无法使用本版已保存结果的输入才需要获取。服务按“服务器已有原件 → 本版可复用计算结果 → 指定来源下载”处理资源请求，资源状态以服务器为准。

## 2. 下载位置和数据布局

旧脚本 `public_data_chasing_lightning/scripts/remote_start_download_plan.sh` 记载 GNSS 项目根为 `/home/xnchen/gnss_public_data/chasing_lightning`；这是本地保存的历史部署依据，本次没有连接服务器确认其当前状态。第一轮工作台记录仅确认 Web 项目内 `runtime/demo.sqlite3` 和 `runtime/materials/`，不能据此推定 Web 项目绝对路径。

本次确定以下部署布局，实施时先定位并复用旧根目录；新运行目录由服务器配置创建，不搬运到笔记本：

| 内容 | .16 位置与用途 |
|---|---|
| 原始资产根 `SERVER_ASSET_ROOT` | `/home/xnchen/gnss_public_data/chasing_lightning`，复用旧 GNSS 布局；回放的 `REPLAY_ASSET_ROOT` 指向此根 |
| 高频原始文件 | `${SERVER_ASSET_ROOT}/raw/cddis_highrate/YYYY/DDD/HH/` |
| 日原始文件 | `${SERVER_ASSET_ROOT}/raw/cddis_daily/YYYY/DDD/` |
| 下载与解压临时区 | `${SERVER_ASSET_ROOT}/tmp/early_warning/`，按任务分目录 |
| 工作台运行根 `SERVER_RUNTIME_ROOT` | 目标默认 `/home/xnchen/gnss_public_data/early_warning_runtime`；此目录是待部署配置，不是已确认存在的目录 |
| 在线数据库和材料 | `${SERVER_RUNTIME_ROOT}/online/`，接续已有在线数据库后使用，不另建空库替代既有记录 |
| 历史回放实例 | `${SERVER_RUNTIME_ROOT}/replays/<instance_id>/`，与在线状态分开，原始文件可共用 |
| 资源台账与保留档案 | `${SERVER_RUNTIME_ROOT}/catalog/`、`evidence/`、`reports/`；分别保存下载/引用状态、可引用结果和报告 |

服务器可用磁盘挂载变化时修改根路径配置，前端不依赖固定物理盘。公开案例清单继续使用相对路径和资源 ID。原始文件、凭据、运行数据库不进入 Git 仓库。前端默认下载的是报告或精简结果；“获取原始数据”动作由服务器执行，页面显示其服务器保存位置。

## 3. 自动获取与持续监测

复用现有下载器 `skills/igs-cddis-api-download/scripts/igs_cddis_api.py` 以及已配置的来源访问方式，接入现有 FastAPI、SQLite 任务和后台调度。旧下载器支持日期、站点、小时、并发、失败重试和跳过已完成文件；当前未完成的 `.part` 会重下，不能把它描述成已有字节级断点续传。旧命令覆盖输出 manifest 的行为也不能替代工作台的持久资源台账。

默认调度要求：

| 来源 | 默认发现周期 | 获取范围 |
|---|---:|---|
| GNSS high-rate | 每 15 分钟 | 已配置站点、已结束且来源已发布的切片；日常持续监测按所选时段持续获取，不沿用双案例每天半小时的演示限制 |
| GNSS daily | 每 1 小时 | 已配置站点最近 3 天中尚缺的完整日文件，兼顾来源延迟发布 |
| PortWatch | 每 24 小时 | 沿用最近 90 日的增量获取和修订处理；历史补缺明确指定日期，不能用“相对今天”替代 |
| 新闻 | 每 5 分钟 | 沿用现有来源配置与覆盖续接 |

上述周期是系统检查周期，不承诺来源按这个时效发布数据。页面同时显示来源观测时间、最后成功获取时间和积压。订阅参数首次保存后自动运行，正常采集不依赖创建会话或逐文件点击。

资源流程为：发现/缺失 → 等待下载 → 下载中 → 原件可用 → 计算中 → 结果已保存 → 待分析 → 已分析；原件随后可以过期清理。资源台账保存来源、站点、观测窗、采样间隔、文件名/来源版本、字节数、位置、尝试次数、下次重试时间、处理版本及引用关系。下载完成只表示原件到达，不能直接标为“已计算”或“正常”。

最多同时下载 2 个 GNSS 文件，默认同时解析 1 个；取得一个文件就处理并保存结果，不等待整个历史批次下载齐再计算。以资源和来源版本去重，在线与回放共用同一份可复用原件。已有本版结果且原件已清理时，直接读取结果；需要新版重新计算时才重新取得原件。Qwen 在结果/证据版本变化后入队，重复拉取和缓存清理不触发重复分析。

连接失败沿用 60 秒起、最高 1 小时的指数退避及来源 `Retry-After`。尚未发布的切片记为等待来源；源端确认无数据的记录显示覆盖缺口。来源凭据不可用时显示具体来源不可用，其他来源继续工作。下载和 GNSS 处理的未完成工作在服务器重启后续接，成功工作保持完成。字节级续传仅在下载器与来源支持时使用，否则重下单个未完成文件，不重下整个案例。

笔记本离线不影响服务器；`.16` 自身无法访问数据源时，只能处理已到达数据，页面标为获取滞后。连接恢复后从持久覆盖位置补缺，同时优先处理最新已发布数据；缺口单独保留，不以扫描到较新日期冒充中间完整覆盖。服务器重启后的应用常驻恢复应沿用其部署管理方式，不依赖 SSH 窗口存活；应用恢复不要求重启既有模型服务。

## 4. 前端必须补齐的内容

在现有三栏工作台中增加“数据获取与存储”入口，复用已有看板布局。它是服务器任务的配置和状态视图，浏览器不运行下载器。

| 区域 | 必须显示或支持的内容 |
|---|---|
| 来源与订阅 | 来源、站点、daily/high-rate、监测日期/时段、采集周期、自动采集开关；默认配置自动运行 |
| 历史补缺 | 选择哈尔科夫/霍尔木兹或明确站点日期，显示服务器已有、已有计算结果、待下载及来源无数据；可发起定向补取 |
| 下载进度 | 等待、下载、计算、分析、失败/重试数量；已下载字节，来源提供总量时显示百分比，否则标为未知；最近成功与下次重试 |
| 存储情况 | 原始缓存、临时文件、指标、数据库/证据、报告分别占用多少；缓存额度、服务器保存位置、最早/最新数据时间 |
| 保留规则 | 当前各类文件保留期、清理周期、上次/下次清理、释放字节数和清理原因；可调整规定范围内的额度和期限 |
| 维护动作 | 暂停/恢复来源、重试失败、补取指定范围、执行到期/可回收缓存清理；不影响已保存的分析结果 |
| 原件已清理后的证据详情 | 明示“原文件已清理，指标与报告保留”，仍可看曲线、来源、时间、处理版本和实际工具证据；可请求服务器重取原件 |

持续运行无需操作这些按钮。前端打开、关闭或切换案例，不改变后台采集进度。维护操作作用于服务器持久任务，页面刷新后状态仍在。

## 5. 缓存期限和清理周期

以下作为待实现的默认值，均由服务器配置持久保存并在前端展示。**保留期限与清理频率是两件事：原件可保留数天，清理任务每小时执行。**

| 数据类型 | 默认保留规则 | 清理时机 |
|---|---|---|
| 正在下载的部分文件 | 活跃下载保留；任务停止且最后写入超过 24 小时的残片可回收，任务保留并重新排队 | 每小时 |
| 解压后的巨大 RINEX/中间数组 | 只保留当前计算所需副本，结果持久保存后立即释放；解析失败也释放临时解压副本，压缩原件按失败规则处理 | 文件处理结束立即清理；重启后回收无任务使用的遗留临时副本 |
| 已成功计算的压缩 GNSS 原件 | 从计算结果与必要证据成功保存起保留 72 小时 | 每小时清理到期文件 |
| 已被异常记录关联的原件 | 如原件仍在，保留至计算完成/首次异常关联中较晚时刻起 7 天；反复查看不延长期限 | 每小时；不为延长留存自动重下已经清理的原件 |
| 未处理或处理失败的原件 | 默认最多等待 7 天；正常等待期间用于续接。到期仍无法处理时先记录资源缺口及重取依据，再回收非活跃原件 | 每小时；仍在计算或下载中的文件不按到期删除 |
| GNSS 完整分窗特征缓存 | 保留 90 天，年龄按本次计算时间计；活动历史基线和未完成工作所需结果继续保留 | 每日 03:30（Asia/Shanghai） |
| 日汇总、站点元数据、基线摘要、PortWatch 日表及其证据版本 | 长期保留，不随原件缓存到期删除；保持可计算的统计和历史差值依据 | 不设原始缓存 TTL，前端单列占用 |
| 报告、异常历史、所引用的精简指标窗口、真实工具结果与关键模型依据 | 长期保留；包括已清理原件的来源、窗口、单位、版本、清理时间及原始资源标识 | 不参与原始缓存清理 |
| 普通运行日志、未被结果引用的冗长请求响应 | 30 天；去重/续接所需任务状态和已引用证据继续保留 | 每日 03:30（Asia/Shanghai） |

“只留报告”不能退化成只留一段模型文字：后续比较仍需要数值基线、单位、覆盖与原始引用，曲线和工具结果也需要可引用的精简数据。这部分与报告一起长期保存，大体积原始观测和无须长期保留的完整数组按上述期限回收。原件删除后保留的是当次计算依据；若来源未来不再提供原件，应明确无法重新做原始解析。

新下载的原始文件与临时区共享默认 **50 GiB** 缓存额度。占用达到 **45 GiB** 时提前回收最早的可回收、已完成处理原件，目标降至 **35 GiB**；因此 72 小时/7 天是正常容量下的保留期，容量回收可提前结束原件保留，台账记录实际原因。活动下载、解析和必须读取原件的工作不在回收对象内。可回收量不足时暂停新增 GNSS 大文件下载并显示“等待缓存空间”，优先完成已有计算；新闻、日表和模型分析按各自可用资源继续，不能通过删除未落盘的结果或在线数据库腾空间。

清理仅作用于系统资源台账管理的缓存文件。旧 GNSS 项目中的既有原件可以直接读取复用；是否纳入新清理策略须在接管时记录，不能对整个旧 `raw/` 或整个用户目录按日期批量删除。在线和回放共享的原件按双方实际使用情况释放，由同一个清理任务协调。保留档案的占用另外统计，不把 50 GiB 原始缓存额度误报为整个项目的存储上限。

## 6. 两例缺项怎样自动补齐

| 案例/数据 | 服务器侧动作 |
|---|---|
| 哈尔科夫 GNSS | 按清单定位旧根下 32 个观测日文件和 58 个事前参考文件；已有直接复用，缺少者按各资源记录的实际来源定向获取，不能一律改用不同来源或假定全部需要重下 |
| 霍尔木兹 GNSS | 同样定位 42 个观测切片和 12 个参考切片，按三站、指定日期及 10:00/10:15 名义窗口补缺；本例半小时范围不扩大为整日下载 |
| 霍尔木兹 PortWatch | 在服务器已存日表中先查 `chokepoint6` 的 2026-01-30～02-13；缺的日期通过现有来源按绝对日期补取，与已有 02-14～03-05 合并并保留修订信息 |

补齐完整 28 日参考窗口需要上述 15 日；现有 13 日距离至少 21 个有效日的最低门槛还差 8 个有效日。不能用 0 填缺日，也不能把后来的日期当作事前参考。确认某原件源端不可取得时保存真实缺口，继续处理有数据的站点和另一案例。

当前回放代码把 `missing/failed` 资源跳过且可能结束案例，需要改成与资源任务联动：下载或重新计算成功后重新激活未完成资源和受影响的原批次，沿用原 `as_of`，保留已完成工作，不能要求用户换新数据库才能使用补到的数据。下载推进依赖的数据状态和业务结论分开保存。

## 7. 实施顺序和交付口径

1. 在 `.16` 定向定位旧原件、现有下载配置和工作台目录，使用服务器资源台账替代笔记本文件存在性判断。
2. 接入持久下载任务、缺失自动补取和 GNSS 在线数据到达处理；先复用下载器与现有后端，再把两例历史缺项通过同一入口补齐。
3. 同步实现下载/存储页面、缓存期限与引用后的清理，使获取、计算、保留、回收形成一条可持续运行的流程。
4. 在执行授权范围内部署并运行两例，记录实际获取/复用数量、计算与分析结果、前端依据；持续监测的运行证据另按实际发生记录，不能由历史回放推定已实现全年无间断监测。

后续交付至少应证明：浏览器关闭不决定任务推进；新增真实资料自动进入分析；服务器补到的数据可续接原案例；到期原件能清理且结果、基线和引用仍可查看；两例的成功、缺口及技术失败分别可追踪。只记录真实执行过的业务流程，不为说明文档追加压力测试、故障注入或无关校验。

本次只更新需求和交付报告，没有启动下载、删除服务器文件或改变 `.16` 的运行状态。
