# 第三轮交付：自动采集、Qwen3 分析与异常看板

本轮依据 `codex_round3_autonomous_monitoring_pipeline.md` 实现服务常驻的自动监测流程，替代交互式任务设计。应用启用后根据保存配置采集公开新闻和民用航运宏观数据，新材料及证据变化自动进入 Qwen3 分析，异常记录自动发布和更新。日常运行无需提问、创建会话、点击分析或逐条确认。

## 交付状态

| 项目 | 本轮状态 |
|---|---|
| 代码与文档 | 已写入现有 FastAPI、SQLite、采集、模型客户端和三栏前端 |
| .16 部署及重启 | 未执行；当前服务器不会因本次代码修改自动升级 |
| 实际采集及模型调用 | 未执行，没有本轮真实业务结果 |
| 测试、smoke test、故障注入 | 未执行，不声称已通过 |

部署和真实运行仍未进行。本文件描述代码提供的行为，不以第一轮样本或第二轮规则记录证明第三轮链路已经运行。

## 实现位置

| 文件 | 作用 |
|---|---|
| `app/config.py`、`.env.example` | 默认监测范围、来源周期、回看与分析预算、退避参数及流水线总开关 |
| `app/main.py` | 生命周期启动和关闭流水线；配置、看板、异常与工作详情接口 |
| `app/pipeline.py` | 来源调度、持久队列消费、补读业务函数、异常发布、失败退避与重启续接 |
| `app/sources.py` | 新闻候选检索和正文读取；GDELT 分窗覆盖续接、真实 RSS feed 信息、来源重试时间；复用 PortWatch 日序列采集 |
| `app/store.py` | 事务与仅首次插入、材料版本及运行关联、待分析工作查询和保存 |
| `app/llm.py` | `assess_update()` 统一分析、真实引用与结构检查、有限补读和模型请求预算 |
| `app/runner.py` | 与旧入口共用单个模型请求锁；来源锁与模型等待分离；忙碌请求不返回别人的运行 ID |
| `app/monitoring.py` | 沿用第二轮数值规则的触发、冻结参考、恢复和修订语义 |
| `app/static/` | 自动监测默认入口、流水线状态、来源配置、异常动态和证据详情；保留旧主题维护与历史案例 |

继续使用一个 Web worker、一个 Qwen 请求消费者和现有 SQLite 数据目录。没有增加 Redis、Celery、聊天主流程、外部通知服务或模型训练组件。

## 配置与启动

沿用原 `.env`、Web 端口、`DATA_DIR` 和独立模型服务。启动入口仍为：

```bash
python -m app
```

默认模型接口为 `http://127.0.0.1:8000/v1`，模型名为 `qwen3-4b`。Web 应用不管理模型环境或 GPU；保留现有 8192-token 模型上下文设置。既有部署不要用 `.env.example` 覆盖 `.env`。

首次生成配置的主要默认值：

```dotenv
PIPELINE_ENABLED=true
PIPELINE_NEWS_TOPIC='(Hormuz OR "Strait of Hormuz") (shipping OR tanker OR cargo OR maritime)'
PIPELINE_RSS_TERMS=Hormuz,shipping,tanker,maritime
PIPELINE_SOURCES=gdelt,rss,portwatch
PIPELINE_NEWS_INTERVAL=300
PIPELINE_PORTWATCH_INTERVAL=86400
PIPELINE_LOOKBACK_HOURS=48
PIPELINE_OVERLAP_MINUTES=30
PIPELINE_BATCH_SIZE=3
```

新闻主题、RSS 关键词、允许来源和周期首次写入 SQLite。后续在看板更新，暂停状态也保存于 SQLite，重启继续使用。修改新闻主题或 RSS 筛选会建立新的监测范围记录，保留之前的材料与异常。旧试验主题和历史回放不会被批量激活，自动调度仅使用流水线持有的 `pipeline_owned` 监测。

`PIPELINE_ENABLED=false` 是环境总开关；它关闭自动派发，即使保存的暂停状态为恢复也不会启动。看板总暂停只阻止新工作派发，已返回的材料和结果仍保存。关闭浏览器不会暂停后台。

其余工程参数在启动时读取，完整示例见 [.env.example](.env.example)：

| 配置 | 默认值及含义 |
|---|---|
| `PIPELINE_ANALYSIS_VERSION` | `shipping_v1`，分析配置版本，参与工作去重 |
| `PIPELINE_TOOL_CALLS` | `2`，每批最多两次实际补读 |
| `PIPELINE_MODEL_REQUESTS` | `4`，每批最多四次模型请求，格式修复也计入 |
| `PIPELINE_SOURCE_CONCURRENCY` | `2`，同时推进的来源检查数量 |
| `PIPELINE_SEARCH_WINDOWS` | `8`，每次 GDELT 检查最多请求的时间窗数量 |
| `PIPELINE_RETRY_SECONDS` | `60`，首次退避秒数 |
| `PIPELINE_RETRY_MAX_SECONDS` | `3600`，指数退避上限；来源或模型要求的更长 `Retry-After` 仍优先遵守 |

新闻批大小最多为 3，可配置为更小值。批大小只约束模型处理，不限制来源本轮能保存的材料数量。PortWatch 使用现有 `PORTWATCH_*` 与 `PW_*` 配置，默认霍尔木兹海峡、最近 90 个自然日。

## 启动后的自动行为

1. **恢复保存状态。** 应用启动调度器和分析消费者，将中断的 `running` 工作及未完成来源检查恢复为待处理状态；已完成的输入版本保持完成。
2. **按来源采集。** 新闻默认每 5 分钟检查，PortWatch 默认每 24 小时检查；同来源和范围的采集不会重叠。来源采集不占用 Qwen 请求锁。
3. **保存材料与工作。** 新闻候选返回后先保存 `pipeline_buffer`，正文读取进度随之保存，暂停或重启后续接已经返回的材料。材料版本、待分析工作和采集进度在同一事务内保存。队列使用仅首次插入，重复 HTTP 获取不会重置已经完成的工作。
4. **自动调用 Qwen。** 新闻最多 3 条一批；PortWatch 将一轮新增或修订及程序结果整理为一份精简报告。不会先额外生成逐条新闻摘要，也不会给 90 个日期分别调用模型。
5. **自动发布。** 分析结果与异常发布、工作完成标记在同一事务内保存。看板下一轮读取即可显示新增、持续、修订、解除或待分析状态。
6. **继续运行。** 本批无新线索或证据不足也完成保存；服务继续等新材料。异常不是来源停止的条件，浏览器关闭期间的记录继续保留。

GDELT 按实际收录时间排序，初始回看 48 小时，后续从已覆盖位置向前重叠 30 分钟去重。单窗达到来源返回上限时继续分窗；本次请求预算内未完成的时间窗以 `uncovered_windows` 保留，下次续接，不将整个窗口冒充为已覆盖。

无法继续细分的满额窗单列为 `gaps`。完整覆盖位置 `covered_until` 不越过缺口，扫描位置 `scanned_until` 可以继续前进以发现后续新资料，保留缺口说明。

RSS 只覆盖 `RSS_URL` 当前实际列出的条目，使用真实 feed 标题、链接和日期。筛选为标题或摘要匹配任一配置关键词。默认 UN News 是联合国新闻 feed，不是完整航运新闻库；其历史覆盖为 `unknown`。没有日期的条目保留未知时间，不补造发布时间。未配置来源显示不可用，其他来源继续运行。

PortWatch 继续使用 `collect_portwatch_series()`、`upsert_portwatch()` 和 `monitoring.evaluate()`。只有实际数值、版本、规则或业务证据变化才产生新分析工作，仅获取时刻变化不会触发。前 28 个自然日的有效值中位数、至少 21 个有效日、连续两日低于冻结参考的 50% 触发及连续两日达到 80% 恢复，均沿用第二轮语义。首次初始化只发布末尾仍活跃的提醒。

## 分析输入、输出与异常记录

每条持久工作包含 `id`、范围、材料引用、分析版本、创建时间、`status`、尝试次数和 `next_retry_at`。新闻工作键区分范围、材料 ID、内容版本和分析版本；数值工作按保存的实际输入版本推进修订编号。队列按不可变创建时间处理。

`assess_update(inputs, existing_alerts, config, tool_handler)` 接收本批新闻或数值输入、少量同范围既有异常以及精简的程序计算结果。模型正文有共享长度预算，不传入 90 天原始 JSON、全部历史报告或完整日志。完整材料链接和历史证据留在数据库供详情读取。

返回值包含 `assessments`、模型名、耗时、请求记录和实际工具结果。每个输入恰好有一条判断：

| `decision` | 保存含义 |
|---|---|
| `no_anomaly` | 本批资料未提供需发布的新线索，不扩写为整个地区没有异常 |
| `candidate` | 材料直接支持的新公开报道线索，附真实引用 |
| `update` | 新证据补充、更正或否定给定的同范围既有线索 |
| `insufficient_evidence` | 当前文本、覆盖或补读预算不足，保存缺口并等待新输入 |

模型只能引用实际提供的材料 ID，更新目标只能来自本次给定异常列表。结构和引用检查不等于已经证实模型判断准确。新闻的 `resolved` 或 `revoked` 只用于有材料支持的明确恢复或更正更新，没有新报道不能解除线索。

必要补读只有 `read_material` 与 `search_news`：前者读取本批已提供的真实材料，后者使用程序限定的现有来源、范围、回看窗口和条数。它们直接调用业务函数，不调用自己的 HTTP 接口，也不会将自身任务重新排队。预算耗尽保存证据不足，没有等待用户的 `need_input` 状态。

数值异常使用 `origin_type=numeric_rule` 并沿用稳定提醒 ID；新闻使用 `origin_type=news_clue`。程序规则证据与 Qwen 分析分别保存，来源修订保留旧依据并将旧结论标记为过时。Qwen 不能改写数值、阈值或程序状态。模型不可用时数值规则命中仍发布为待分析，之后自动重试。

材料及异常分别保存来源观测日期、已知发布时间、系统首次获取时间、分析完成时间和实际发布时间。日度或滞后来源不显示为分钟级现场观测。已读标记只改变看板未读提示，不影响采集或业务状态。

## 失败、续接与前端接口

工作状态包括 `queued`、`running`、`completed` 和 `retry_wait`，失败原因、尝试次数及下次重试时间持久化。连接、超时和 HTTP 故障按指数退避处理，尊重数字或 HTTP 日期形式的 `Retry-After`。一次格式修复后仍非法或输出截断会进入单条重试；持续失败项隔离延后，后续工作仍可继续。重启自动恢复未完成工作，不要求人工恢复每条任务。

模型服务的整体重试时间保存为 `model_retry_at`；等待期间不由其他批次绕过退避，来源采集继续推进。

前端沿用三栏和轻量轮询，约每 2 秒刷新可见页面。顶部显示运行或暂停、最近采集、最近分析、积压与来源日期；这些是保存的运行记录，不冒充即时健康探测。中部展示异常及已有数值曲线，右侧展开程序依据、Qwen 结论、材料版本、原始链接和时间。没有新异常时仍同时显示积压或故障。

| 接口 | 用途 |
|---|---|
| `GET /api/pipeline` | 保存的配置、来源状态、队列数量、近期异常和工作摘要 |
| `POST /api/pipeline/config` | 修改 `paused`、`news_topic`、`rss_terms` 及来源开关、`interval_seconds` |
| `GET /api/pipeline/alerts` | 近期自动异常摘要 |
| `GET /api/alerts/{alert_id}` | 异常完整依据和模型结论 |
| `POST /api/alerts/{alert_id}/read` | 设置或取消已读 |
| `GET /api/work/{work_id}` | 对应输入、判断、请求记录和工具结果 |
| `GET /api/materials/{material_id}` | 实际材料版本，可读取跨主题复用的原始证据 |
| `GET /api/monitors/{monitor_id}/metrics` | 已保存的 PortWatch 数值、曲线和规则状态 |

常规轮询不返回全部历史模型请求；展开对应工作或运行时才读取详情。旧资料摘要、手动更新及历史回放仍作为维护入口保留，自动流水线不依赖这些入口。
