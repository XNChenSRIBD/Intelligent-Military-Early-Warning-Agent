# Round9：离线数值解释验证结果

**验证已完成：通过现有 SSH 临时转发模型端口，收到 Qwen3-4B 的 16 份真实回答。** 直接读表的 A 条件有 **4 题明确事实或逻辑错误**；附带程序事实的 B 条件有 **1 题明确错误**。B 在 N1 的大小关系和 N4 的增幅计算上答对了，C4 的日期比较逻辑仍然出错。还有若干题只答了一部分，不能记作完整通过。

主结果：[ROUND9_NUMERIC_EXPLANATION_RESULTS.json](ROUND9_NUMERIC_EXPLANATION_RESULTS.json)。每题保留实际输入、程序事实、HTTP 原响应、模型原答和五项检查意见；没有改写模型回答。

## 1. 八题 A/B 对比

A 为原始表和问题；B 为相同表和问题，额外给出程序计算关系。以下“未答完整”与“明确错误”分别计数。

| 题目 | A：直接读表 | B：附带程序事实 |
|---|---|---|
| **N1：单侧边界** | **明确错误。** 把 A 两窗说成高于下界、B 首窗低于上界，三处全反。实际是 22.719 < 29.4848、22.219 < 33.469、38.25 > 33.4284。 | **完整通过。** 三处方向正确；说明没有总体检验不改变已知关系，也未补造另一侧边界。 |
| **N2：例窗与汇总** | **明确错误。** 说“已有观测的大小关系未明确说明”，但三个例窗与下界已给出；也没写清两对象各窗口的方向。 | **待复核。** 说对了例窗低于参考，但未写清 A 四窗低、B 前两低后两高。“已有观测的下界与参考比较”混用了观测值和参考下界，保留原句，不算完整通过。 |
| **N3：两对象异向与元数据** | **未答完整。** A 低、B 高正确，但未交代 C:S1P、dB-Hz、1 秒、同窗，以及原因证据为空。 | **未答完整。** 同样说对方向，仍遗漏具体元数据和原因缺项；反而谈“其他对象的单位缺口”。 |
| **N4：名义容量变化** | **明确错误。** 增量 102,271 算对，增幅却写成 **714.43%**；只说与历史差距大，未说明最终值更低。 | **完整通过。** 增量 **102,271**、增幅 **71,518.18%**、最终 102,414 低于两个历史值均正确，始终比较容量字段。 |
| **C1：参考组与中位差** | **未答完整。** 两组 289 窗和中位差 +0.150／−0.565 正确，也正确否定“中位数接近零就表示每窗在范围内”。遗漏参考天数及上下偏窗口计数。 | **未答完整。** 原答与 A 相同，核心结论正确，仍遗漏 1 天／3 天和 136 低／153 高、104 低／89 高。 |
| **C2：有效比例与窗口覆盖** | **未答完整。** 8/29 是正确的精确比例，18 秒内没有缺历元、不能推成全天也说对了；没有说明分母 29 来自整份文件的卫星集合。 | **未答完整。** 补上正确的 **27.59%**，但仍用“总可能值”代替分母的具体含义。 |
| **C3：单位换算与空值** | **未答完整。** 保留 40 receiver_units，却只说 CNR_P10 值未确认；没有解释换算缺项，也没有区分零值、缺文件和缺观测。 | **未答完整。** 补充了到 dB-Hz 的换算依据未确认；仍未用 900 历元、17 信号明确交代文件和观测存在、原值并非零。 |
| **C4：相邻日期比较** | **明确错误。** +1、−1 算对，却说“存在数值冲突”。 | **明确错误。** 2→3、3→2 均写对，仍说“两份说明中数值变化方向不同，存在数值冲突”。这是两个相邻日期范围，可以同时成立。 |

N3 没有明确声称本题两对象单位未知，C3 也没有明确声称文件缺失；这里记录的是遗漏，不另造错误原句。C1 是已修正内容的保持检查，其原历史末答状态不变。

## 2. 实际计数与判断依据

| 本次 8 题中的结果 | A | B |
|---|---:|---:|
| 完整通过五项检查 | 0 | 2 |
| 明确事实或逻辑错误 | 4 | 1 |
| 未答完整 | 4 | 4 |
| 有字段表述歧义，待复核 | 0 | 1 |
| JSON 结构符合约定 | 8 | 8 |
| 引用的行编号真实存在 | 8 | 8 |

前四行各自合计 8。**没有完整通过，不等于回答全错。** C1、C2 等题的核心判断正确，缺的是题目要求的具体支持证据或字段定义。

全部 16 份原答已逐条阅读。五项检查分别为：数字和比较关系、是否答完原问题、单位／窗口／组别／缺项、是否额外加入无依据的显著性／原因／范围推广、引用是否对应真实输入行。N2/B 的歧义原句和未回答部分已保存；其余明确遗漏直接记为未答完整。没有用关键词或 JSON 解析成功代替语义判断。N2/A 虽引用了真实汇总行，却漏掉原问题的关键数字行；真实行编号也不保证推导正确。

**在这组有限任务中，程序提供关系后，模型叙述与事实更一致。** N1 和 N4 展示了这点。与此同时，C4 表明：即使涨跌已算好，模型仍可能误解比较问题；N3、C1、C2、C3 则仍有回答不完整的问题。这里仅报告八题的原始计数，不推算普遍准确率或统计显著改善。

## 3. 实际运行依据

- 执行时间：**2026-09-14 12:38:58 至 12:39:18，北京时间**，约 20.29 秒。
- 实际返回模型：所有回答均为 **qwen3-4b**。固定 `temperature=0`、`max_tokens=1800`、`enable_thinking=false`、`response_format=json_object`，超时 120 秒。
- 本次真实推理请求 **16 次**，每题每条件一次；收到 **16 份**回答，无格式修复、额外推理重试或提示词改版。请求顺序为 N1/A、N1/B，依次到 C4/A、C4/B。
- 接口报告用量：输入 **7,459 tokens**，输出 **1,191 tokens**，合计 **8,650 tokens**。
- 原脚本、题目、原始数字、程序事实和检查预期均未因这次接通而修改。来源映射和检查预期没有送入模型。

连接问题已查明：服务器的 8000 端口实际监听 **127.0.0.1:8000**。笔记本直连 `.16:8000` 无法使用这个本机监听；现有 SSH 转发接通了同一服务。用户明确要求快速解决连接并开始验证后，本次临时转发 `127.0.0.1:18009 → sribd-16:127.0.0.1:8000`，运行结束已关闭转发，没有修改服务器监听或环境配置。

之前两次直连失败分别保存在 [首次记录](ROUND9_NUMERIC_EXPLANATION_RESULTS_DIRECT1.json)和[再次尝试记录](ROUND9_NUMERIC_EXPLANATION_RESULTS_RETRY1.json)。含这两次连接失败，累计 HTTP 尝试为 18 次，其中真正收到模型回答的是本次 16 次；没有把失败连接计成模型答题。

## 4. 代码与复用方式

独立工具仍为两个脚本：[facts.py](../../tools/numeric_explanation_check/facts.py) 计算大小关系、差值与比例；[run.py](../../tools/numeric_explanation_check/run.py) 构造固定 A/B 请求并保存原答。使用 Python 标准库，不导入原应用模块。

输入与溯源分别保存在 [inputs.json](../../tools/numeric_explanation_check/inputs.json)、[source_mapping.json](../../tools/numeric_explanation_check/source_mapping.json)和[review_expectations.json](../../tools/numeric_explanation_check/review_expectations.json)。程序事实带输入行引用，汇总及元数据标明来自输入声明。计算按行字段执行，不按题号或对象名返回预设答案。

复用时，先保持以下临时转发连接：

```powershell
ssh.exe -N -o BatchMode=yes -o PasswordAuthentication=no -o ExitOnForwardFailure=yes -L 127.0.0.1:18009:127.0.0.1:8000 sribd-16
```

另一个 PowerShell 在仓库根目录执行：

```powershell
$env:MODEL_BASE_URL = 'http://127.0.0.1:18009/v1'
& 'D:/anaconda3/python.exe' -B tools/numeric_explanation_check/run.py --execute --connection-profile 'C:/Users/NaNF2P/.codex/skills/connect-sribd-server/SKILL.md' --output reports/round9/ROUND9_NUMERIC_EXPLANATION_RESULTS_NEW.json
```

上述新输出文件名供另行安排的运行使用；现有结果不会被覆盖。结束后关闭转发连接。本次实际运行由临时启动程序管理转发并自动关闭，只执行了原定的 16 项。

## 5. 本轮结论与保留问题

离线数值解释验证已完成；N2/B 保留具体原句待复核，其他错误和未答完整项按实际结果交付，不追加请求争取全通过。

A/B 使用的是匿名、简化输入，未复现 Round6 的完整上下文，不能据此认定历史错误的根因。B 的关系由程序计算，模型负责叙述，改善不能算作模型独立学会了计算。本轮未接入或修复原在线解释模块。

原两例仍为 `original_goal_status=not_demonstrated`、`strict_lead_hours=null`，原 `analysis_explanation_issue`、Round6／7／8 文档、历史模型原答和业务判断均保留。环境原因、站间变化原因、地域代表性及历史可得性不在八题范围内。本次没有改动原监测应用、8080／8081、前端、阈值、队列或运行数据库。
