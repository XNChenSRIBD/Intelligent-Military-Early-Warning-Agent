# 第二轮：PortWatch 日度监测与持续偏低提醒

代码基线：`5a4d5b6accc15cd246b7db3781bf6c88d9a61b71`。

## 交付状态

本轮已编写日度序列、规则状态、持续提醒、数值前端和按需解释代码。未部署到 .16；未启动或重启工作台、模型服务；未执行采集、模型调用、测试、额外校验或 smoke test。因此本文件没有第二轮功能的真实业务运行记录，也不将第一轮的三条 PortWatch 样本作为第二轮提醒结果。

## 修改及配置

| 文件 | 对应功能 |
|---|---|
| `app/sources.py` | 完整日序列采集、明确分页、原始字段与日期查询链接 |
| `app/store.py` | 追加日度日期键，关联原材料版本，保留当前值及有效性 |
| `app/monitoring.py` | 参考中位数、候选、持续提醒、恢复、初始化与来源修订 |
| `app/runner.py` | 独立数值路径、提醒持久化、按需解释共享单任务约束 |
| `app/main.py` | 指标、提醒、材料版本与解释接口 |
| `app/llm.py` | 复用原连接的提醒解释入口，保留新闻分析契约 |
| `app/config.py`、`.env.example` | 集中规则默认参数 |
| `app/static/` | PortWatch 数值视图、提醒列表、证据和解释交互 |
| `README.md` | 操作及启动说明 |

`PORTWATCH_URL` 与 `PORTWATCH_ID` 沿用原配置。本轮获取窗口为最近 90 个自然日，完整获取其中来源实际返回的记录，未返回的日期不补零。新闻材料条数及回看小时数不再控制 PortWatch 数值历史。

| 配置 | 默认值 |
|---|---:|
| `PW_RULE_ID` | `pw_lowflow_v1` |
| `PW_HISTORY_DAYS` | 90 |
| `PW_BASELINE_DAYS` | 28 |
| `PW_MIN_BASELINE_DAYS` | 21 |
| `PW_TRIGGER_RATIO` | 0.5 |
| `PW_TRIGGER_DAYS` | 2 |
| `PW_RECOVERY_RATIO` | 0.8 |
| `PW_RECOVERY_DAYS` | 2 |

阈值是首版工程默认值，没有准确率或误报率校准依据。规则参数变更应采用新规则标识，保留旧提醒的判定快照。

## 数据与规则

追加的 `portwatch_daily` 以来源、海峡、源日期为唯一键，保存 `n_total`、船型计数、名义运力、原始 attributes、材料编号与版本、有效性以及首次见到和最近获取时间。原值再次出现时，当前记录会重新指向该内容实际对应的材料版本，不按最大版本号猜测当前值。

`monitor_state` 和 `alert` 复用原 `records` 表。旧内容仍可通过材料版本和提醒证据查看。建表只追加结构，现有材料、数据库和 `.env` 均保留。数值材料标为 `not_required`，不进入新闻待分析循环。

`monitor_state` 保存实际来源、规则快照、最新观测、当前状态、候选及冻结参考、已处理日期和输入材料版本。`alert` 保存触发与恢复观测、首次记录及解除时刻、参考样本、当前证据版本、首次证据、修订前证据和解释状态。

首日候选冻结前 28 日有效计数的中位数；参考不足 21 日或中位数为零时不做比例判定。连续两日低于冻结参考的 50% 触发，连续两日达到 80% 解除；同日重查不推进状态，缺日打断连续候选和恢复计数。已有提醒在缺口期间保留。阈值之间的值表示尚未达到持续恢复条件。

初始化按已保存的源日期顺序得到末尾状态，只发布仍活跃的一条初始化提醒。后续批量新日期按日期推进。来源修订会重算相关片段、更正或撤销提醒，保留原始证据和修订前证据。证据改变会增加版本并令旧解释失效；仅获取时刻更新不增加证据版本。

观测依据日期、系统实际记录提醒或解除的时刻分开保存。来源发布时间未知时保持空值。提醒不会声称在历史观测日期当时已被发出。

## 页面与接口

在在线页选择 PortWatch 主题即可进入数值监测视图。左侧显示真实海峡、规则窗口与参数；中部显示最新计数、参考与比例、规则状态、日曲线及提醒列表；右侧展开触发日数值、冻结参考、参考样本、恢复条件、原材料版本、原查询链接和解释。

来源修订记录以简短状态变化展示，首次证据及修订前证据可折叠展开，并可访问当时引用的材料版本。新建 PortWatch 任务默认每 1440 分钟更新，仍需用户开启周期更新；已有任务的周期和启停状态保留。

曲线按 `observed_date` 绘制，缺日断线；参考线及触发/解除线随实际规则状态绘制。历史运行选择保留，但数值区明确展示当前指标与提醒，未将其伪称为历史运行快照。

新增读取接口：

```text
GET  /api/monitors/{monitor_id}/metrics
GET  /api/monitors/{monitor_id}/alerts
GET  /api/alerts/{alert_id}
GET  /api/materials/{material_id}
POST /api/alerts/{alert_id}/explain
```

读取接口不会触发采集或模型调用。PortWatch 更新复用既有 monitor/run/schedule 接口；来源失败只记为来源与运行失败，保留此前业务判断。

## Qwen 与启动

Qwen 只在用户请求提醒解释时运行。输入为已保存证据包中的规则、程序计算值、触发与恢复观测及参考材料编号，省略原始 attributes 和冗长查询链接。输出是中文解释及材料引用，不能修改程序保存的规则或状态。

解释与采集继续共用单任务机制。相同证据版本的完成解释直接读取；失败保留数值判断与错误，用户可以再次请求。原新闻摘要流程保留。

启动仍使用 `python -m app`、`scripts/start_demo.sh` 或 `scripts/start_demo.ps1`，一个 worker、原 Web 端口、原数据目录与独立模型服务。后续获授权部署时需更新工作台代码并重启该应用，使追加表和新接口生效；无需重启 Qwen、调整 GPU 或升级依赖。不要使用示例配置覆盖部署中的 `.env`。

本轮功能已写入代码；部署与实际业务运行留待用户另行授权。
